# 問題10：最大曲率時のリダイレクション不全

## 問題の症状

- **最大曲率ゲイン（0.133 rad/m、CURVATURE_RADIUS=7.5m）でアバターが直進する**
- **緩い曲率では正常に動作する**
- **ログ上はすべて正常に見える**
- **視覚的な錯覚ではなく、実際に直進している**
- **APF矢印とシアン軌跡は正しい方向を示している**

## 調査の経緯

### 初期調査（2026-01-09）

**追加したデバッグログ:**

1. **PredRedLPP_Redirector.cs:555:**
   ```csharp
   Debug.Log($"[PredRedLPP] Applying Curvature: {action.primaryValue:F3}, isWalking={redirectionManager.isWalking}, deltaPos.magnitude={redirectionManager.deltaPos.magnitude:F3}");
   ```

2. **Redirector.cs:87, 95:**
   ```csharp
   // 大きな曲率の場合
   Debug.Log($"[Redirector] SetCurvature: input={originalCurvature:F3}, clamped={curvature:F3}, deltaPos={redirectionManager.deltaPos.magnitude:F3}, rotationGC={rotationInDegreesGC:F2}°, prevRotation={rotationInDegrees - rotationInDegreesGC:F2}°, applied={applied}");

   // isWalking=false の場合
   Debug.LogWarning($"[Redirector] SetCurvature: NOT APPLIED (isWalking=false), curvature={curvature:F3}");
   ```

**修正内容:**

1. **ApplyCurvatureの符号修正:**
   - `Trajectory.cs:360`: `currentAngle -= rotationAngle` → `currentAngle += rotationAngle`

2. **曲率のクランプ処理追加:**
   - `TrajectoryEvaluator.cs:273-283`: `ApplyActionToTrajectory()` で曲率を事前にクランプ

3. **CURVATURE_RADIUSの修正:**
   - Unity Scene: 5.14m → 7.5m（論文Table 2準拠）
   - 最大曲率: 0.195 rad/m → 0.133 rad/m

### 第1回ログ分析

**発見:**
1. 最大曲率のアクションは継続的に選ばれている ✅
2. isWalking=Trueの時は正常に適用されている ✅
3. **3フレーム後にリアクティブモードに切り替わる** ⚠️
   - 最大曲率を3フレーム適用 → 予測軌跡の終端が障害物と衝突
   - `feasibleTrajectories.Count == 0` → リアクティブモードへ

**第1回修正（衝突判定の緩和）:**
- `Trajectory.cs:140`: `CheckCollisionWithObstacles()`に`checkRatio`パラメータを追加
- `LemniscatePathPredictor.cs:229`: 軌跡の最初の50%のみ衝突判定（`checkRatio=0.5f`）

### 第2回ログ分析

**発見:**
1. 最大曲率のアクションは継続的に選ばれている ✅
2. isWalking=Trueになっている ✅
3. リアクティブモードに切り替わっていない ✅
4. **しかし依然として直進する** ⚠️
5. SetCurvature()内部のログがないため詳細不明

**第2回修正（詳細ログの追加）:**
- `Redirector.cs:84-87`: SetCurvature()で最大曲率の場合のログ追加
  - rotationInDegreesGC（計算された回転角度）
  - applied（曲率が実際に適用されたか）
- `Redirector.cs:39-42`: ApplyGains()で回転のログ追加

### 第3回ログ分析

**発見:**
1. `SetCurvature()`は正常に動作 ✅
   - rotationGC = -0.19° ～ -0.24°（正常な値）
   - applied = True
   - `rotationInDegrees`に値が設定されている
2. **しかし`ApplyGains()`のログが一度も出ていない** ⚠️
   - ログ閾値0.3°なのに、rotationGC=-0.19°～-0.24°でログが出ないのは異常

**第3回修正（ApplyGains()ログ閾値の引き下げ）:**
- `Redirector.cs:39`: ログ閾値を0.3° → 0.01°に変更

### 第4回ログ分析

**発見:**
1. `ApplyGains()`は正常に呼ばれている ✅
   - rotation = 0.14° ～ 0.16°/frame
   - 50fps想定で 7.5°/秒、75°/10秒
2. `prevRotation=0.00°`が毎回
   - `ClearGains()`が毎フレーム呼ばれる（正常な動作）
3. **システムは完全に正常動作している** ✅

**第4回修正（すべての曲率のログを出力）:**
- `Redirector.cs:84`: ログ閾値を0.12 → 0.01に変更
- `PredRedLPP_Redirector.cs:314`: ログを"HIGH Curvature" → "Curvature"に変更、閾値を0.12 → 0.01に

**第5回修正（ログの簡潔化）:**
- `GlobalConfiguration.cs:560`: スタックトレースを無効化
  ```csharp
  Application.SetStackTraceLogType(LogType.Log, StackTraceLogType.None);
  ```

### 第5回ログ分析（矛盾の発見）

**ログは完全に正常動作を示している:**
1. 最大曲率 `curvature=0.133 rad/m` が継続的に選択 ✅
2. `isWalking=True` ✅
3. `SetCurvature()`: `applied=True` ✅
4. `rotationGC = 0.27° ～ 0.36°/frame` ✅
5. `ApplyGains()`: rotation が正常に適用 ✅
6. 理論値との照合:
   - 平均回転速度: 0.3°/frame × 50fps = 15°/秒
   - 理論値（CURVATURE_RADIUS=7.5m、移動速度2m/s）: 15.3°/秒
   - **完全に一致** ✅

**しかし実際の動作:**
- APF矢印が右方向を向いている
- シアン軌跡も右方向に向かわせる予測軌跡を生成
- ログでは回転が適用されている
- **にも関わらずアバターは直進する** ❌

**重要な条件:**
- **最大曲率ゲイン（0.133 rad/m）が加えられた場合に限る**
- **緩い曲率では正常にアバターがカーブしながら進む**
- これは視覚的な錯覚ではなく、アルゴリズムのロジック通りの動きをしていない

### 並進ゲイン削除テスト（2026-01-10）

**実験内容:**
- `RedirectionActionFactory`から並進ゲインとNull actionを削除
- 最大曲率の軌跡が選択されているのに直進する原因を特定

**実験結果:**
- ✅ 並進ゲインとNull actionは選択されなくなった
- ❌ **しかし問題は解決せず、依然として直進する**

**発見:**
- コスト関数で最大曲率の軌跡が正しく選択される ✅
- シアン色の予測軌跡が最大限カーブした軌跡を描画する ✅
- APF矢印も壁を避ける方向を示す ✅
- ログでは`SetCurvature()`が正常に動作（`applied=True`）✅
- ログでは`ApplyGains()`が正常に回転を適用 ✅
- **にも関わらず、アバターは実際には直進する** ❌

**推測:**
- `ApplyGains()`で回転を適用した直後に、何らかの処理が回転を上書き・キャンセルしている可能性
- 最大曲率の時のみ発生することから、特定の閾値を超えた場合に発動する処理がある可能性

## 根本原因の特定（2026-01-11）

### 段階的テスト結果

**テスト結果:**
- `gc = ±0.027 rad/m` (曲率半径 37.5m): **アバターが正常に曲がる** ✅
- `gc = ±0.080 rad/m` (曲率半径 12.5m): **アバターが直進してしまう** ❌
- `gc = ±0.133 rad/m` (曲率半径 7.5m): **アバターが直進してしまう** ❌

### 問題の本質

**曲率値が約0.027～0.080 rad/mの間の閾値を超えると、リダイレクションが機能しなくなる。**

これは特定の曲率値（最大値のみ）の問題ではなく、**曲率の大きさそのもの**に依存する問題。

### 考えられる根本原因

#### 1. SimulatedWalkerの回転補正閾値

**ファイル:** `Assets/OpenRDW/Scripts/Movement/Motion Simulators/SimulatedWalker.cs:15`

```csharp
const float ROTATIONAL_ERROR_ACCEPTED_IN_DEGRESS = 1;
```

- 大きな曲率による累積回転が、この閾値を超えて補正されている可能性
- `gc=0.027`: `rotationGC ≈ 0.14°/frame` → 累積しても小さい
- `gc=0.080`: `rotationGC ≈ 0.40°/frame` → 数フレームで1°を超える

#### 2. フレーム間の回転の打ち消し

- SimulatedWalkerが毎フレームwaypointに向かって回転補正
- 大きな曲率による回転は「waypointからのずれ」として認識され、次のフレームで補正される
- 小さな曲率（0.027）は補正閾値内に収まるため蓄積される

## 暫定対策の実装（2026-01-11）

### 1. アクションセットの制限

**ファイル:** `Assets/OpenRDW/Scripts/Redirection/Prediction/RedirectionAction.cs`

**通常版（203-210行目付近）:**
```csharp
// 0.1 rad/mを超える曲率を除外
for (int i = 0; i < numSteps; i++)
{
    float gc = Mathf.Lerp(-1f / config.CURVATURE_RADIUS, 1f / config.CURVATURE_RADIUS, t);
    if (Mathf.Abs(gc) <= 0.1f)
    {
        actions.Add(new RedirectionAction(RedirectionGainType.Curvature, gc));
    }
}
```

**結果:**
- CURVATURE_RADIUS = 7.5mの場合: 4種類（±0.080, ±0.027）
- しかし `gc=±0.080` でも問題が発生

### 2. 複合ゲインの無効化

**ファイル:** `Assets/OpenRDW/Scripts/Redirection/Prediction/RedirectionAction.cs`

複合ゲインを一時的にコメントアウト（問題の切り分けのため）

## 関連ファイル一覧

### アクションセット生成
- **RedirectionAction.cs** (`Assets/OpenRDW/Scripts/Redirection/Prediction/RedirectionAction.cs`)
  - `GenerateActionSet()`: 203-228行目
  - `GenerateMinimalActionSet()`: 237-258行目

### 軌跡評価・シミュレーション
- **TrajectoryEvaluator.cs** (`Assets/OpenRDW/Scripts/Redirection/Prediction/TrajectoryEvaluator.cs`)
  - `ApplyActionToTrajectory()`: 267行目～
  - 273行目: `maxCurvature = 1f / globalConfiguration.CURVATURE_RADIUS`

- **Trajectory.cs** (`Assets/OpenRDW/Scripts/Redirection/Prediction/Trajectory.cs`)
  - `ApplyCurvature()`: 332行目～
  - `CheckCollisionWithObstacles()`: 140行目

- **LemniscatePathPredictor.cs** (`Assets/OpenRDW/Scripts/Redirection/Prediction/LemniscatePathPredictor.cs`)
  - `FilterFeasibleTrajectories()`: 229行目

### リダイレクター
- **PredRedLPP_Redirector.cs** (`Assets/OpenRDW/Scripts/Redirection/Redirectors/PredRedLPP_Redirector.cs`)
  - `InjectRedirection()`: 203行目～
  - `ApplyReactiveRedirection()`: 588行目～
  - `ApplyRedirectionByNegativeGradient()`: 688行目～
  - 483行目, 714行目: `SetCurvature(desiredSteeringDirection * 1f / globalConfiguration.CURVATURE_RADIUS)`

- **Redirector.cs** (`Assets/OpenRDW/Scripts/Redirection/Redirectors/Redirector.cs`)
  - `SetCurvature()`: 73行目～
  - `ApplyGains()`: 35行目～

### シミュレーター
- **SimulatedWalker.cs** (`Assets/OpenRDW/Scripts/Movement/Motion Simulators/SimulatedWalker.cs`)
  - 15行目: `const float ROTATIONAL_ERROR_ACCEPTED_IN_DEGRESS = 1`
  - `TurnAndWalkToWaypoint()`: 90行目～
  - `WalkIfPossible()`: 121行目～

### 設定
- **GlobalConfiguration.cs** (`Assets/OpenRDW/Scripts/Others/GlobalConfiguration.cs`)
  - 111行目: `public float CURVATURE_RADIUS = 7.5f`

## 現在のステータス

**根本原因:** 曲率値の閾値問題（0.027以下は正常動作）

**暫定対策:** アクションセットで0.1以下に制限済み（しかし0.080でも問題発生）

**次のステップ:**
1. 曲率を0.027以下に制限
2. SimulatedWalkerの回転補正閾値を調査
3. より大きな曲率を使用可能にする根本的な解決策を検討

## 追加の未解決問題（2026-01-11時点）

**現象:** アクションセットで`gc=±0.027`を選択しても、**シアンの軌跡（最良軌跡）がめちゃくちゃ曲がって表示される**

**ログ:**
```
[PredRedLPP] Curvature Selected: curvature=0.027
[Redirector] SetCurvature: curvature=0.027
[Redirector] SetCurvature: curvature=-0.133  ← 直後に0.133が設定される
```

**推測:** リアクティブリダイレクション（483行目, 714行目）で`1f / globalConfiguration.CURVATURE_RADIUS`が呼ばれている可能性
