# 問題10：最大曲率時のリダイレクション不全

## 問題の症状

- **曲率を大きく加えた軌跡が最良軌跡T_redとなるときアバターが直進する**
- **緩い曲率では正常に動作する**
- **ログ上はすべて正常に見える**
- **視覚的な錯覚ではなく、実際に直進している**
- **APF矢印とシアン軌跡は正しい方向を示している**

## 調査の経緯

### 初期調査（2026-01-09）

**追加したデバッグログ:**

1. **PredRedLPP_Redirector.cs:555:**
   ```csharp
   Debug.Log($"[PredRedLPP] Applying Curvature: {action.primaryValue:F3}, isWalking={redirectionManager.isWalking}, deltaPos.magnitude={redirectionManager.deltaPos.magnitude:F3}");
   ```

2. **Redirector.cs:87, 95:**
   ```csharp
   // 大きな曲率の場合
   Debug.Log($"[Redirector] SetCurvature: input={originalCurvature:F3}, clamped={curvature:F3}, deltaPos={redirectionManager.deltaPos.magnitude:F3}, rotationGC={rotationInDegreesGC:F2}°, prevRotation={rotationInDegrees - rotationInDegreesGC:F2}°, applied={applied}");

   // isWalking=false の場合
   Debug.LogWarning($"[Redirector] SetCurvature: NOT APPLIED (isWalking=false), curvature={curvature:F3}");
   ```

**修正内容:**

1. **ApplyCurvatureの符号修正:**
   - `Trajectory.cs:360`: `currentAngle -= rotationAngle` → `currentAngle += rotationAngle`

2. **曲率のクランプ処理追加:**
   - `TrajectoryEvaluator.cs:273-283`: `ApplyActionToTrajectory()` で曲率を事前にクランプ

3. **CURVATURE_RADIUSの修正:**
   - Unity Scene: 5.14m → 7.5m（論文Table 2準拠）
   - 最大曲率: 0.195 rad/m → 0.133 rad/m

### 第1回ログ分析

**発見:**
1. 最大曲率のアクションは継続的に選ばれている ✅
2. isWalking=Trueの時は正常に適用されている ✅
3. **3フレーム後にリアクティブモードに切り替わる** ⚠️
   - 最大曲率を3フレーム適用 → 予測軌跡の終端が障害物と衝突
   - `feasibleTrajectories.Count == 0` → リアクティブモードへ

**第1回修正（衝突判定の緩和）:**
- `Trajectory.cs:140`: `CheckCollisionWithObstacles()`に`checkRatio`パラメータを追加
- `LemniscatePathPredictor.cs:229`: 軌跡の最初の50%のみ衝突判定（`checkRatio=0.5f`）

### 第2回ログ分析

**発見:**
1. 最大曲率のアクションは継続的に選ばれている ✅
2. isWalking=Trueになっている ✅
3. リアクティブモードに切り替わっていない ✅
4. **しかし依然として直進する** ⚠️
5. SetCurvature()内部のログがないため詳細不明

**第2回修正（詳細ログの追加）:**
- `Redirector.cs:84-87`: SetCurvature()で最大曲率の場合のログ追加
  - rotationInDegreesGC（計算された回転角度）
  - applied（曲率が実際に適用されたか）
- `Redirector.cs:39-42`: ApplyGains()で回転のログ追加

### 第3回ログ分析

**発見:**
1. `SetCurvature()`は正常に動作 ✅
   - rotationGC = -0.19° ～ -0.24°（正常な値）
   - applied = True
   - `rotationInDegrees`に値が設定されている
2. **しかし`ApplyGains()`のログが一度も出ていない** ⚠️
   - ログ閾値0.3°なのに、rotationGC=-0.19°～-0.24°でログが出ないのは異常

**第3回修正（ApplyGains()ログ閾値の引き下げ）:**
- `Redirector.cs:39`: ログ閾値を0.3° → 0.01°に変更

### 第4回ログ分析

**発見:**
1. `ApplyGains()`は正常に呼ばれている ✅
   - rotation = 0.14° ～ 0.16°/frame
   - 50fps想定で 7.5°/秒、75°/10秒
2. `prevRotation=0.00°`が毎回
   - `ClearGains()`が毎フレーム呼ばれる（正常な動作）
3. **システムは完全に正常動作している** ✅

**第4回修正（すべての曲率のログを出力）:**
- `Redirector.cs:84`: ログ閾値を0.12 → 0.01に変更
- `PredRedLPP_Redirector.cs:314`: ログを"HIGH Curvature" → "Curvature"に変更、閾値を0.12 → 0.01に

**第5回修正（ログの簡潔化）:**
- `GlobalConfiguration.cs:560`: スタックトレースを無効化
  ```csharp
  Application.SetStackTraceLogType(LogType.Log, StackTraceLogType.None);
  ```

### 第5回ログ分析（矛盾の発見）

**ログは完全に正常動作を示している:**
1. 最大曲率 `curvature=0.133 rad/m` が継続的に選択 ✅
2. `isWalking=True` ✅
3. `SetCurvature()`: `applied=True` ✅
4. `rotationGC = 0.27° ～ 0.36°/frame` ✅
5. `ApplyGains()`: rotation が正常に適用 ✅
6. 理論値との照合:
   - 平均回転速度: 0.3°/frame × 50fps = 15°/秒
   - 理論値（CURVATURE_RADIUS=7.5m、移動速度2m/s）: 15.3°/秒
   - **完全に一致** ✅

**しかし実際の動作:**
- APF矢印が右方向を向いている
- シアン軌跡も右方向に向かわせる予測軌跡を生成
- ログでは回転が適用されている
- **にも関わらずアバターは直進する** ❌

**重要な条件:**
- **最大曲率ゲイン（0.133 rad/m）が加えられた場合に限る**
- **緩い曲率では正常にアバターがカーブしながら進む**
- これは視覚的な錯覚ではなく、アルゴリズムのロジック通りの動きをしていない

### 並進ゲイン削除テスト（2026-01-10）

**実験内容:**
- `RedirectionActionFactory`から並進ゲインとNull actionを削除
- 最大曲率の軌跡が選択されているのに直進する原因を特定

**実験結果:**
- ✅ 並進ゲインとNull actionは選択されなくなった
- ❌ **しかし問題は解決せず、依然として直進する**

**発見:**
- コスト関数で最大曲率の軌跡が正しく選択される ✅
- シアン色の予測軌跡が最大限カーブした軌跡を描画する ✅
- APF矢印も壁を避ける方向を示す ✅
- ログでは`SetCurvature()`が正常に動作（`applied=True`）✅
- ログでは`ApplyGains()`が正常に回転を適用 ✅
- **にも関わらず、アバターは実際には直進する** ❌

**推測:**
- `ApplyGains()`で回転を適用した直後に、何らかの処理が回転を上書き・キャンセルしている可能性
- 最大曲率の時のみ発生することから、特定の閾値を超えた場合に発動する処理がある可能性

## 根本原因の特定（2026-01-11）

### 段階的テスト結果

**テスト結果:**
- `gc = ±0.027 rad/m` (曲率半径 37.5m): **アバターが正常に曲がる** ✅
- `gc = ±0.080 rad/m` (曲率半径 12.5m): **アバターが直進してしまう** ❌
- `gc = ±0.133 rad/m` (曲率半径 7.5m): **アバターが直進してしまう** ❌

### 問題の本質

**曲率値が約0.027～0.080 rad/mの間の閾値を超えると、リダイレクションが機能しなくなる。**

これは特定の曲率値（最大値のみ）の問題ではなく、**曲率の大きさそのもの**に依存する問題。

### 考えられる根本原因

#### 1. SimulatedWalkerの回転補正閾値

**ファイル:** `Assets/OpenRDW/Scripts/Movement/Motion Simulators/SimulatedWalker.cs:15`

```csharp
const float ROTATIONAL_ERROR_ACCEPTED_IN_DEGRESS = 1;
```

- 大きな曲率による累積回転が、この閾値を超えて補正されている可能性
- `gc=0.027`: `rotationGC ≈ 0.14°/frame` → 累積しても小さい
- `gc=0.080`: `rotationGC ≈ 0.40°/frame` → 数フレームで1°を超える

#### 2. フレーム間の回転の打ち消し

- SimulatedWalkerが毎フレームwaypointに向かって回転補正
- 大きな曲率による回転は「waypointからのずれ」として認識され、次のフレームで補正される
- 小さな曲率（0.027）は補正閾値内に収まるため蓄積される

## 暫定対策の実装（2026-01-11）

### 1. アクションセットの制限

**ファイル:** `Assets/OpenRDW/Scripts/Redirection/Prediction/RedirectionAction.cs`

**通常版（203-210行目付近）:**
```csharp
// 0.1 rad/mを超える曲率を除外
for (int i = 0; i < numSteps; i++)
{
    float gc = Mathf.Lerp(-1f / config.CURVATURE_RADIUS, 1f / config.CURVATURE_RADIUS, t);
    if (Mathf.Abs(gc) <= 0.1f)
    {
        actions.Add(new RedirectionAction(RedirectionGainType.Curvature, gc));
    }
}
```

**結果:**
- CURVATURE_RADIUS = 7.5mの場合: 4種類（±0.080, ±0.027）
- しかし `gc=±0.080` でも問題が発生

### 2. 複合ゲインの無効化

**ファイル:** `Assets/OpenRDW/Scripts/Redirection/Prediction/RedirectionAction.cs`

複合ゲインを一時的にコメントアウト（問題の切り分けのため）

## 関連ファイル一覧

### アクションセット生成
- **RedirectionAction.cs** (`Assets/OpenRDW/Scripts/Redirection/Prediction/RedirectionAction.cs`)
  - `GenerateActionSet()`: 203-228行目
  - `GenerateMinimalActionSet()`: 237-258行目

### 軌跡評価・シミュレーション
- **TrajectoryEvaluator.cs** (`Assets/OpenRDW/Scripts/Redirection/Prediction/TrajectoryEvaluator.cs`)
  - `ApplyActionToTrajectory()`: 267行目～
  - 273行目: `maxCurvature = 1f / globalConfiguration.CURVATURE_RADIUS`

- **Trajectory.cs** (`Assets/OpenRDW/Scripts/Redirection/Prediction/Trajectory.cs`)
  - `ApplyCurvature()`: 332行目～
  - `CheckCollisionWithObstacles()`: 140行目

- **LemniscatePathPredictor.cs** (`Assets/OpenRDW/Scripts/Redirection/Prediction/LemniscatePathPredictor.cs`)
  - `FilterFeasibleTrajectories()`: 229行目

### リダイレクター
- **PredRedLPP_Redirector.cs** (`Assets/OpenRDW/Scripts/Redirection/Redirectors/PredRedLPP_Redirector.cs`)
  - `InjectRedirection()`: 203行目～
  - `ApplyReactiveRedirection()`: 588行目～
  - `ApplyRedirectionByNegativeGradient()`: 688行目～
  - 483行目, 714行目: `SetCurvature(desiredSteeringDirection * 1f / globalConfiguration.CURVATURE_RADIUS)`

- **Redirector.cs** (`Assets/OpenRDW/Scripts/Redirection/Redirectors/Redirector.cs`)
  - `SetCurvature()`: 73行目～
  - `ApplyGains()`: 35行目～

### シミュレーター
- **SimulatedWalker.cs** (`Assets/OpenRDW/Scripts/Movement/Motion Simulators/SimulatedWalker.cs`)
  - 15行目: `const float ROTATIONAL_ERROR_ACCEPTED_IN_DEGRESS = 1`
  - `TurnAndWalkToWaypoint()`: 90行目～
  - `WalkIfPossible()`: 121行目～

### 設定
- **GlobalConfiguration.cs** (`Assets/OpenRDW/Scripts/Others/GlobalConfiguration.cs`)
  - 111行目: `public float CURVATURE_RADIUS = 7.5f`



## 追加の未解決問題（2026-01-11時点）

**現象:** アクションセットで`gc=±0.027`を選択しても、**シアンの軌跡（最良軌跡）がめちゃくちゃ曲がって表示される**

**ログ:**
```
[PredRedLPP] Curvature Selected: curvature=0.027
[Redirector] SetCurvature: curvature=0.027
[Redirector] SetCurvature: curvature=-0.133  ← 直後に0.133が設定される
```

**推測:** リアクティブリダイレクション（483行目, 714行目）で`1f / globalConfiguration.CURVATURE_RADIUS`が呼ばれている可能性

## SimulatedWalker回転補正閾値テスト（2026-01-11）

### 試行内容
`SimulatedWalker.cs:15`の`ROTATIONAL_ERROR_ACCEPTED_IN_DEGRESS`を変更してテスト

**仮説:**
- この閾値（元の値: 1度）がリダイレクションによる回転を妨げている
- 大きな曲率による回転が1度を超えると、waypointへの補正が優先されて相殺される

**テスト:**
1. `ROTATIONAL_ERROR_ACCEPTED_IN_DEGRESS = 10` → ❌ 効果なし
2. `ROTATIONAL_ERROR_ACCEPTED_IN_DEGRESS = 45` → ❌ 効果なし
3. `ROTATIONAL_ERROR_ACCEPTED_IN_DEGRESS = 180` → ❌ 効果なし

**結果:**
- **この閾値は問題の原因ではない**
- 最大値（180度）に設定しても、最大曲率時に直進する問題は解決せず
- `ROTATIONAL_ERROR_ACCEPTED_IN_DEGRESS`を元の1度に戻した

**次の調査対象:**
- `SimulatedWalker.cs:96`の`RotateIfNecessary()`がリダイレクションの回転を直接打ち消している可能性
- `TurnAndWalkToWaypoint()`のフロー全体を調査

## RotateIfNecessary()無効化テスト（2026-01-11）

### 試行内容
`SimulatedWalker.cs:96`の`RotateIfNecessary()`をコメントアウトして、waypointへの回転補正を無効化

**仮説:**
- リダイレクションが回転を適用 → 次フレームで`RotateIfNecessary()`がwaypointに向かって回転補正 → 相殺される
- `RotateIfNecessary()`を無効化すれば、リダイレクションの回転が有効になる

**結果:**
- ❌ **再生直後にフリーズ**
- `isWalking=false`のまま188件のログが出力され、無限ループに陥った

**原因:**
- `RotateIfNecessary()`を無効化すると、アバターが最初のwaypointに向かって回転できない
- `ROTATIONAL_ERROR_ACCEPTED_IN_DEGRESS = 1`の条件（WalkIfPossible内の125行目）を満たせない
- 移動が開始されず、`isWalking=false`のまま → リダイレクションが適用されない
- **結論: `RotateIfNecessary()`は必須の機能**

**元に戻した:**
- `SimulatedWalker.cs:96`を元の状態に戻した

## 対策案A: 回転速度の減速テスト（2026-01-11）

### 試行内容
`SimulatedWalker.cs:105`の`RotateIfNecessary()`内の回転速度を10%に減速

**仮説:**
- リダイレクションの回転（約0.3°/frame）が、waypoint補正の回転で相殺される
- waypoint補正を10%に減速すれば、リダイレクションの回転が蓄積される

**結果:**
- ❌ **効果なし**
- 最大曲率時に依然として直進する

**元に戻した:**
- `SimulatedWalker.cs:105`を元の状態に戻した

## 対策案B: 曲率適用中のフラグテスト（2026-01-11）

### 試行内容
曲率適用中を示すフラグ`IsCurvatureApplied`を追加し、曲率適用中はRotateIfNecessary()をスキップ

**仮説:**
- 曲率適用中のみRotateIfNecessary()をスキップすれば、リダイレクションの回転が妨げられない

**修正箇所:**
1. `Redirector.cs:21`: `public bool IsCurvatureApplied { get; private set; }`を追加
2. `Redirector.cs:38`: ClearGains()でフラグをリセット
3. `Redirector.cs:91`: SetCurvature()で曲率適用時にフラグを立てる
4. `SimulatedWalker.cs:97-100`: フラグがtrueの時はRotateIfNecessary()をスキップ

**結果:**
- ❌ **効果なし**
- 最大曲率時に依然として直進する

**元に戻した:**
- 全ての変更を元の状態に戻した

## アルゴリズム間比較分析（2026-01-11）

### ThomasAPF_Redirectorの動作（正常に曲がる）

**処理フロー:**
1. `InjectRedirection()`が毎フレーム呼ばれる
2. `ApplyRedirectionByNegativeGradient(ng)`を呼び出す
3. 常に最大曲率を設定: `SetCurvature(desiredSteeringDirection * 1f / globalConfiguration.CURVATURE_RADIUS)`
4. `ApplyGains()`で回転を適用
5. **アバターが正常に曲がる** ✅

**特徴:**
- シンプルなリアクティブアルゴリズム
- 毎フレーム同じロジックで曲率を決定
- 予測や軌跡評価は行わない

### PredRedLPP_Redirectorの動作（直進してしまう）

**処理フロー:**
1. `InjectRedirection()`が毎フレーム呼ばれる
2. 予測軌跡を生成（レムニスケート + クロソイド）
3. 実行可能な軌跡をフィルタリング
4. T_pred（最良軌跡）を選択
5. アクションセット U から最適なアクションを選択
6. `ApplyRedirectionAction(bestAction)`を呼び出す
7. `SetCurvature(action.primaryValue)`で選択された曲率を設定
8. `ApplyGains()`で回転を適用
9. **ログでは正常だがアバターが直進する** ❌

**特徴:**
- 複雑な予測アルゴリズム
- 軌跡評価とコスト関数でアクションを選択
- ログ上は完全に正常動作

### 重要な発見

**両アルゴリズムの共通点:**
- 同じ`Redirector`基底クラスを継承
- 同じ`SetCurvature()`メソッドを使用
- 同じ`ApplyGains()`メソッドで回転を適用
- 同じ`SimulatedWalker`を使用

**にも関わらず:**
- ThomasAPF: 最大曲率でアバターが曲がる ✅
- PredRedLPP: 最大曲率でアバターが直進する ❌

**ログ分析からの確認事項:**
- PredRedLPPも正しく`SetCurvature(0.133)`を呼び出している
- `rotationGC = 0.27° ～ 0.36°/frame`が計算されている
- `ApplyGains()`で回転が適用されている
- `isWalking=True`になっている
- **ログ上は完全に正常** ✅

### 問題の本質

**ログは正常なのに実際の動作が異なる** という矛盾が存在する。

考えられる原因：
1. **処理順序の問題**: PredRedLPPの処理後に何らかの処理が回転を打ち消している
2. **Transform階層の問題**: PredRedLPPと ThomasAPFで操作しているTransformが異なる
3. **タイミングの問題**: ApplyGains()のタイミングが異なる
4. **未知のフラグ・条件**: PredRedLPP特有の条件でリダイレクションが無効化されている

## 推奨される次の対応策

### 対策案C: デバッグログの強化

**調査項目:**
1. `ApplyGains()`で実際に適用される`transform`の名前をログ出力
2. `transform.RotateAround()`の前後でtransform.rotationをログ出力
3. 毎フレームの最終的なtransform.rotationをログ出力
4. SimulatedWalkerの処理タイミングをログ出力

**目的:**
- ログ上の回転と実際のtransform.rotationの乖離を特定
- どの処理が回転を打ち消しているかを特定

### 対策案D: ThomasAPFとの直接比較

**実験内容:**
1. 同じシーンでThomasAPFとPredRedLPPを交互に切り替えてテスト
2. 同じ条件（同じ位置、同じ曲率）で動作を比較
3. transform.rotationの変化を両アルゴリズムで記録

**目的:**
- 両アルゴリズムの動作の違いを明確化
- PredRedLPP特有の問題を特定

## 対策案C実施結果: transform.rotation追跡ログ（2026-01-11）

### 追加したログ

1. **Redirector.ApplyGains()**: transform.rotationの変化を追跡
   - `transform.name`, `rotationInDegrees`, `yawBefore`, `yawAfter`, `yawDelta`

2. **SimulatedWalker.RotateIfNecessary()**: waypoint補正の回転を追跡
   - `rotationToApply`, `yawBefore`, `yawAfter`, `yawDelta`

### ログ分析結果

**処理順序:**
```
1. SimulatedWalker.RotateIfNecessary (waypoint補正)
2. Redirector.ApplyGains (リダイレクション)
```

**重要な発見: 2つのアバターで動作が異なる**

#### Redirected Avatar_2（正常動作）✅

**10フレーム分の累積:**
- 開始yaw: 231.86°
- 終了yaw: 234.83°
- **累積回転: 2.97°** ✅

**各フレームの動作:**
```
SimulatedWalker: -0.26° ～ -0.37° (waypoint補正、逆回転)
Redirector:      +0.26° ～ +0.37° (リダイレクション)
正味:            yawが確実に増加（約0.3°/frame）
```

#### Redirected Avatar（問題のアバター）❌

**10フレーム分の動作:**
- yaw: 180°付近で停滞
- **Redirector.ApplyGains: -0.05° ～ +0.07°（ほぼゼロ）** ❌
- SimulatedWalker: 正常に動作

**問題の核心:**
- `rotationInDegrees`（ApplyGains()に渡される値）がほぼゼロ
- SetCurvature()は呼ばれているはずだが、rotationInDegreesが設定されていない
- **Redirector.cs:92の`applied`フラグがfalseになっている可能性**

```csharp
bool applied = Mathf.Abs(rotationInDegreesGC) > Mathf.Abs(rotationInDegrees);
```

### 仮説

**Redirected Avatarだけ、別のゲイン（SetRotationGain等）が先に大きな値を設定していて、SetCurvature()のrotationInDegreesGCより大きくなっているため、appliedがfalseになっている。**

考えられる原因：
1. SetRotationGain()で大きな回転ゲインが設定されている
2. 複数のリダイレクション処理が競合している
3. Redirected Avatarだけ特殊な条件が適用されている

### 追加ログ分析: SetCurvature()のapplied状態

**Redirected Avatarのログ（問題のアバター）:**

```
[Redirector.SetCurvature] avatar=Redirected Avatar, curvature=0.027, rotationGC=0.04°～0.05°, prevRotation=0.00°, applied=True
[Redirector.SetCurvature] avatar=Redirected Avatar, curvature=0.080, rotationGC=0.11°～0.13°, prevRotation=0.00°, applied=True
[Redirector.SetCurvature] avatar=Redirected Avatar, curvature=0.133, rotationGC=0.19°～0.25°, prevRotation=0.00°, applied=True
```

**重要な発見:**

1. **applied=True** ✅
   - SetCurvature()は正常に動作している
   - 仮説「appliedがfalseになっている」は否定された

2. **prevRotation=0.00°（毎回）** ✅
   - ClearGains()が正常に呼ばれている
   - SetRotationGain()は呼ばれていない（呼ばれていればprevRotationに値が残る）

3. **rotationGCが小さすぎる** ❌
   - 小曲率（0.027）: rotationGC = 0.04° ～ 0.05°/frame
   - 中曲率（0.080）: rotationGC = 0.11° ～ 0.13°/frame
   - **最大曲率（0.133）: rotationGC = 0.19° ～ 0.25°/frame** ❌

**比較: Redirected Avatar_2（正常動作）の以前のログ:**
- 最大曲率（0.133）: rotationGC = 0.26° ～ 0.37°/frame ✅

### 真の原因が判明

**rotationGCの計算式:**
```csharp
rotationGC = Mathf.Rad2Deg * redirectionManager.deltaPos.magnitude * curvature
```

同じ曲率（0.133）で、rotationGCが異なる理由：

| アバター | rotationGC | 推定deltaPos.magnitude |
|---------|-----------|----------------------|
| Redirected Avatar_2 | 0.26° ～ 0.37° | 0.034m ～ 0.049m |
| **Redirected Avatar** | **0.19° ～ 0.25°** | **0.025m ～ 0.033m** |

**結論:**
- **Redirected Avatarだけ、deltaPos.magnitude（1フレームあたりの移動距離）が約25%～30%少ない**
- これにより、rotationGCも比例して小さくなる
- 小さいrotationGCでは、SimulatedWalkerのwaypoint補正と相殺されて直進してしまう

**考えられる原因:**
1. **並進ゲイン（gt）が小さい**: Redirected Avatarだけ並進ゲインが0.7～0.8程度になっている
2. **移動速度が遅い**: globalConfiguration.translationSpeedが異なる
3. **deltaTimeが異なる**: Redirected Avatarだけフレームレートが低い

### deltaPos.magnitude の実測値

**Redirected Avatarのログ:**

```
小曲率（0.027）:
deltaPos=0.0342m～0.0437m, rotationGC=0.05°～0.07° → アバターが曲がる ✅

中曲率（0.080）:
deltaPos=0.0340m～0.0405m, rotationGC=0.16°～0.19° → アバターが直進する ❌

最大曲率（0.133）の正の場合:
deltaPos=0.0336m～0.0451m, rotationGC=0.26°～0.34° → アバターが直進する ❌

最大曲率（0.133）の負の場合:
deltaPos=0.0125m～0.0243m, rotationGC=-0.10°～-0.19° → アバターが直進する ❌
```

### 問題の本質（2026-01-11 確定）

**重要な発見:**

1. **最大曲率でrotationGCは計算されている**
   - 正の曲率: rotationGC = 0.26°～0.34°/frame
   - これは理論値（0.3°～0.4°/frame）に近い値

2. **しかし、実際にはアバターが直進する**
   - ログ上はrotationGCが設定されている
   - applied=True
   - にも関わらず、視覚的にアバターは直進

3. **負の曲率時にdeltaPosが減少する現象**
   - これは別の問題（減速処理など）
   - 本質的な問題ではない

### 未解決の謎

**ログでは正常なのに、実際の動作は直進**

- SetCurvature(): 正常動作 ✅
- applied=True ✅
- rotationGC = 0.26°～0.34° ✅
- ApplyGains(): 呼ばれているはず ✅
- **実際の動作: 直進** ❌

**考えられる原因:**
1. ApplyGains()の後に何らかの処理が回転を打ち消している
2. Transform階層の問題（別のTransformを操作している）
3. SimulatedWalkerのwaypoint補正が即座に回転を相殺
4. 物理エンジンまたはキャラクターコントローラーの制約

**結論:**
- rotationGCの計算は正常
- SetCurvature()の処理は正常
- しかし、最終的なアバターの回転が適用されていない
- 根本原因は未だ特定できていない

**修正は不要（ユーザー指示）:**
- ドキュメントに記録のみ
