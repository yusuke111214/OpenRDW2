# 問題10：最大曲率時のリダイレクション不全

## 問題の症状

- **曲率を大きく加えた軌跡が最良軌跡T_redとなるときアバターが直進する**
- **緩い曲率では正常に動作する**
- **ログ上はすべて正常に見える**
- **視覚的な錯覚ではなく、実際に直進している**
- **APF矢印とシアン軌跡は正しい方向を示している**

## ログによる調査を行う場合
過去に追加してあるログがある場合は排除して見るべきログ以外は消すようにしてください。

## 解決にあたってのトークン消費
ドキュメントの読み込みや既存スクリプトの調査などで大部分のトークンが使われることが予想されます（既存設計の調査や修正にトークンを使うのは全然良い）。よって少しでもトークン消費を抑えるためにチャット内のまとめや整理は基本最小限にとどめるようにしてください。また、ドキュメントの書き込みでも冗長になりすぎず、端的にまとめることを意識してください。

## Hirtの論文
Hirtの論文はpdfファイルにてこのプロジェクトに存在しています。

## 調査の経緯


### 第1回ログ分析

**発見:**
1. 最大曲率のアクションは継続的に選ばれている ✅
2. isWalking=Trueの時は正常に適用されている ✅
3. **3フレーム後にリアクティブモードに切り替わる** ⚠️
   - 最大曲率を3フレーム適用 → 予測軌跡の終端が障害物と衝突
   - `feasibleTrajectories.Count == 0` → リアクティブモードへ

**第1回修正（衝突判定の緩和）:**
- `Trajectory.cs:140`: `CheckCollisionWithObstacles()`に`checkRatio`パラメータを追加
- `LemniscatePathPredictor.cs:229`: 軌跡の最初の50%のみ衝突判定（`checkRatio=0.5f`）

### 第2回ログ分析

**発見:**
1. 最大曲率のアクションは継続的に選ばれている ✅
2. isWalking=Trueになっている ✅
3. リアクティブモードに切り替わっていない ✅
4. **しかし依然として直進する** ⚠️
5. SetCurvature()内部のログがないため詳細不明

**第2回修正（詳細ログの追加）:**
- `Redirector.cs:84-87`: SetCurvature()で最大曲率の場合のログ追加
  - rotationInDegreesGC（計算された回転角度）
  - applied（曲率が実際に適用されたか）
- `Redirector.cs:39-42`: ApplyGains()で回転のログ追加

### 第3回ログ分析

**発見:**
1. `SetCurvature()`は正常に動作 ✅
   - rotationGC = -0.19° ～ -0.24°（正常な値）
   - applied = True
   - `rotationInDegrees`に値が設定されている
2. **しかし`ApplyGains()`のログが一度も出ていない** ⚠️
   - ログ閾値0.3°なのに、rotationGC=-0.19°～-0.24°でログが出ないのは異常

**第3回修正（ApplyGains()ログ閾値の引き下げ）:**
- `Redirector.cs:39`: ログ閾値を0.3° → 0.01°に変更

### 第4回ログ分析

**発見:**
1. `ApplyGains()`は正常に呼ばれている ✅
   - rotation = 0.14° ～ 0.16°/frame
   - 50fps想定で 7.5°/秒、75°/10秒
2. `prevRotation=0.00°`が毎回
   - `ClearGains()`が毎フレーム呼ばれる（正常な動作）
3. **システムは完全に正常動作している** ✅

**第4回修正（すべての曲率のログを出力）:**
- `Redirector.cs:84`: ログ閾値を0.12 → 0.01に変更
- `PredRedLPP_Redirector.cs:314`: ログを"HIGH Curvature" → "Curvature"に変更、閾値を0.12 → 0.01に

**第5回修正（ログの簡潔化）:**
- `GlobalConfiguration.cs:560`: スタックトレースを無効化
  ```csharp
  Application.SetStackTraceLogType(LogType.Log, StackTraceLogType.None);
  ```

### 第5回ログ分析（矛盾の発見）

**ログは完全に正常動作を示している:**
1. 最大曲率 `curvature=0.133 rad/m` が継続的に選択 ✅
2. `isWalking=True` ✅
3. `SetCurvature()`: `applied=True` ✅
4. `rotationGC = 0.27° ～ 0.36°/frame` ✅
5. `ApplyGains()`: rotation が正常に適用 ✅
6. 理論値との照合:
   - 平均回転速度: 0.3°/frame × 50fps = 15°/秒
   - 理論値（CURVATURE_RADIUS=7.5m、移動速度2m/s）: 15.3°/秒
   - **完全に一致** ✅

**しかし実際の動作:**
- APF矢印が右方向を向いている
- シアン軌跡も右方向に向かわせる予測軌跡を生成
- ログでは回転が適用されている
- **にも関わらずアバターは直進する** ❌

**重要な条件:**
- **緩い曲率では正常にアバターがカーブしながら進む**
- これは視覚的な錯覚ではなく、アルゴリズムのロジック通りの動きをしていない

### 並進ゲイン削除テスト（2026-01-10）

**実験内容:**
- `RedirectionActionFactory`から並進ゲインとNull actionを削除
- 最大曲率の軌跡が選択されているのに直進する原因を特定

**実験結果:**
- ✅ 並進ゲインとNull actionは選択されなくなった
- ❌ **しかし問題は解決せず、依然として直進する**

**発見:**
- コスト関数で最大曲率の軌跡が正しく選択される ✅
- シアン色の予測軌跡が最大限カーブした軌跡を描画する ✅
- APF矢印も壁を避ける方向を示す ✅
- ログでは`SetCurvature()`が正常に動作（`applied=True`）✅
- ログでは`ApplyGains()`が正常に回転を適用 ✅
- **にも関わらず、アバターは実際には直進する** ❌



### PredRedLPP_Redirectorの動作（直進してしまう）

**処理フロー:**
1. `InjectRedirection()`が毎フレーム呼ばれる
2. 予測軌跡を生成（レムニスケート + クロソイド）
3. 実行可能な軌跡をフィルタリング
4. T_pred（最良軌跡）を選択
5. アクションセット U から最適なアクションを選択
6. `ApplyRedirectionAction(bestAction)`を呼び出す
7. `SetCurvature(action.primaryValue)`で選択された曲率を設定
8. `ApplyGains()`で回転を適用
9. **ログでは正常だがアバターが直進する** ❌

**特徴:**
- 複雑な予測アルゴリズム
- 軌跡評価とコスト関数でアクションを選択
- ログ上は完全に正常動作

### 重要な発見

**両アルゴリズムの共通点:**
- 同じ`Redirector`基底クラスを継承
- 同じ`SetCurvature()`メソッドを使用
- 同じ`ApplyGains()`メソッドで回転を適用
- 同じ`SimulatedWalker`を使用

**にも関わらず:**
- ThomasAPF: 最大曲率でアバターが曲がる ✅
- PredRedLPP: 最大曲率でアバターが直進する ❌

**ログ分析からの確認事項:**
- PredRedLPPも正しく`SetCurvature(0.133)`を呼び出している
- `rotationGC = 0.27° ～ 0.36°/frame`が計算されている
- `ApplyGains()`で回転が適用されている
- `isWalking=True`になっている
- **ログ上は完全に正常** ✅

### 問題の本質

**ログは正常なのに実際の動作が異なる** という矛盾が存在する。



---

## エンドポイント可視化による根本原因の特定（2026-01-12）

### エンドポイント可視化の実装

**修正内容:**
- `PredRedLPP_Redirector.cs`にエンドポイント可視化機能を追加
- レムニスケート上の11個のエンドポイントを赤い球体（半径0.05m）で表示
- 毎フレーム、アバターの前方に追従して表示

### エンドポイントの配置パターン

アバターから見て前方を基準とした配置：

```
        F E D C B A B C D E F
        ←――――――――+――――――――→
        左側      中央     右側
```

- **A**: 真正面（直進軌跡）
- **B**: 小さい曲率（左右に2個）
- **C**: 中程度の曲率（左右に2個）
- **D, E, F**: 大きな曲率（左右に各3個）

**レムニスケート方程式（論文Eq.1）:**
```
x(t) = A_Lem × sin(t) / (1 + cos²(t))
y(t) = A_Lem × sin(t)cos(t) / (1 + cos²(t))
t ∈ (0.05, π-0.05)を11個に等間隔分割
```

### 動作確認結果

#### 実際に使用されるエンドポイント

**通常設定（CURVATURE_RADIUS = 7.5m、最大曲率 = 0.133 rad/m）:**

| エンドポイント | 使用状況 | アバターの動作 |
|--------------|---------|--------------|
| A（中央） | ✅ 頻繁に使用 | 正常に直進 ✅ |
| B（左右） | ✅ 頻繁に使用 | 正常に緩やかなカーブ ✅ |
| C（左右） | ⚠️ 使用される | **直進してしまう** ❌ |
| D, E, F（左右） | ❌ ほぼ使用されない | - |

**理由:**
- **D, E, Fが使用されない理由**: 急激な曲がりが必要だが、曲率ゲインの閾値（±0.133 rad/m）を超えるため、コスト関数で選ばれない
- これは**正しい動作**（アバターは物理的にそんなに急激にカーブできない）

#### CURVATURE_RADIUSを変更した場合

**CURVATURE_RADIUS = 5.0m（最大曲率 = 0.2 rad/m）に変更:**

| エンドポイント | 使用状況 | アバターの動作 |
|--------------|---------|--------------|
| A, B | ✅ 使用 | 正常 ✅ |
| C | ✅ 使用 | **正常に曲がる** ✅ |
| D | ⚠️ 使用される | **正常に曲がる** ✅ |
| E, F | ❌ ほぼ使用されない | - |

**CURVATURE_RADIUS = 3.0m（最大曲率 = 0.333 rad/m）に変更:**

| エンドポイント | 使用状況 | アバターの動作 |
|--------------|---------|--------------|
| A, B, C, D | ✅ 使用 | 正常 ✅ |
| E | ⚠️ 使用される | **正常に曲がる** ✅ |
| F | ❌ ほぼ使用されない | - |





### 新たな発見: 軌跡の終端位置の違い

**観察結果:**

| エンドポイント | 軌跡の終端 | アバターの動作 |
|--------------|----------|--------------|
| A, B | **エンドポイントまで届いていない**（レムニスケート内側で終わる） | 正常に動作 ✅ |
| C | **エンドポイントCとほぼ一致**（完全に届く） | 直進してしまう ❌ |

**重要な気づき:**
- 軌跡がエンドポイントまで届いていない場合 → 正常動作
- 軌跡がエンドポイントに届く（一致する）場合 → 直進してしまう

### 根本原因の再考

**問題の本質:**
- 表示されている軌跡はT_red（リダイレクト後の軌跡）
- T_redは`ApplyCurvature()`で変形されるため、エンドポイントからずれる
- 軌跡がエンドポイントに一致する = 変形されていない = 何らかの理由でリダイレクションが適用されていない

**T_pred vs T_red:**

```
T_pred（リダイレクト前の予測軌跡）:
  - クロソイド曲線で定義
  - エンドポイントまで確実に届く
  - コスト評価に使用

T_red（リダイレクト後の軌跡）:
  - T_predに対してApplyCurvature()を適用
  - 曲率ゲインによって変形される
  - 終端位置がエンドポイントからずれる
  - 現在表示されているのはこちら
```

### 対策: 軌跡の終端をエンドポイントまで伸ばす

**実施内容（2026-01-12）:**

1. **並進ゲインと回転ゲインを無効化**
   - `RedirectionAction.cs`: Translation/Rotation/Combinedアクションをコメントアウト
   - Curvatureゲインのみを使用

2. **表示する軌跡をT_predに変更**
   - `TrajectoryEvaluator.cs:255`: `return (bestAction, T_pred);`
   - T_red（変形後）ではなく、T_pred（元の軌跡）を表示
   - これにより、全ての軌跡がエンドポイントまで確実に届く

**期待される結果:**
- シアン色の軌跡が全てのエンドポイントまで届く
- 軌跡の終端がレムニスケート形状の内側で終わることがなくなる
- エンドポイントCが選択された時の挙動を確認可能

---

## T_pred表示による新たな発見（2026-01-12）

### 動作確認結果

**重大な発見: 右側のCのエンドポイントのみが使用されている**

```
エンドポイント配置:
        F E D C B A B C D E F
        ←――――――――+――――――――→
        左側      中央     右側
                          ↑
                      この点のみ使用
```

**観察結果:**

| 状況 | 選択されるエンドポイント | 予測軌跡の表示 |
|-----|----------------------|--------------|
| アバターが直進中 | **右側のC** | 右カーブの軌跡（エンドポイントCまで届く） |
| アバターが左に曲がった後 | **右側のC** | 右カーブの軌跡（エンドポイントCまで届く） |
| アバターが右に曲がった後 | **右側のC** | 右カーブの軌跡（エンドポイントCまで届く） |
| 壁に近づいている時 | **右側のC** | 右カーブの軌跡（エンドポイントCまで届く） |

**重要な気づき:**
- **過去の移動方向に関わらず、99%右側のCが選ばれる**
- 他の10個のエンドポイント（左側5個、中央A、右側B,D,E,F）は**ほぼ使用されない**
- アバターの過去の動きが予測軌跡の選択に反映されていない

### 考えられる原因

#### 原因1: MSEベースの軌跡選択の問題

**関連コード:** `LemniscatePathPredictor.cs:268-298`

```csharp
public Trajectory SelectBestTrajectoryUsingSimilarityMeasure(
    List<Trajectory> trajectories,
    Queue<Vector3> positionHistory)
{
    // MSE（平均二乗誤差）で軌跡を選択
    float mse = trajectory.CalculatePathSimilarity(positionHistory);
}
```

**問題の可能性:**
- MSE計算が正しく機能していない
- 常に右側のCの軌跡が最小MSEとして選ばれてしまう
- 過去10フレームの位置履歴との類似度計算に何らかのバグがある

**検証方法:**
- 各軌跡のMSEスコアをログ出力
- どの軌跡が最小MSEなのかを確認
- MSE計算ロジックを検証

#### 原因2: 衝突判定フィルタリングの問題

**関連コード:** `LemniscatePathPredictor.cs:218-242`

```csharp
public List<Trajectory> FilterFeasibleTrajectories(
    List<Trajectory> trajectories,
    SingleSpace space)
{
    // 障害物との衝突をチェック
    bool hasCollision = trajectory.CheckCollisionWithObstacles(...);

    // トラッキング空間内に収まっているかチェック
    bool withinBounds = trajectory.IsWithinTrackingSpace(...);
}
```

**問題の可能性:**
- 11本の軌跡が生成される
- フィルタリングで10本が除外される
- 右側のCのみが「実行可能」として残る
- その結果、選択の余地がなく、常に右側のCが選ばれる

**検証方法:**
- `FilterFeasibleTrajectories()`の入力と出力をログ出力
- 生成された軌跡数: 11本
- フィルタリング後の軌跡数: ?本
- 除外された軌跡とその理由をログ

#### 原因3: レムニスケート形状の左右非対称

**関連コード:** `LemniscatePathPredictor.cs:165-207`

```csharp
private List<Vector2> GenerateLemniscatePoints(
    Vector2 origin,
    Vector2 direction,
    float size,
    int numPoints)
{
    // レムニスケート方程式
    float x_local = size * sinT / denom;
    float y_local = -size * sinT * cosT / denom;  // 符号反転（190行目）
}
```

**問題の可能性:**
- レムニスケート方程式の実装にバグがある
- y_localの符号反転（190行目）により、意図しない配置になっている
- 左右対称のはずが、右側に偏っている

**検証方法:**
- 生成された11個のエンドポイント座標をログ出力
- 左右対称になっているか確認
- 中央（A）が原点の前方にあるか確認

### 影響

**問題の深刻度:**

1. **予測が機能していない**
   - 「過去の移動パターンから未来を予測する」というPredRedLPPの核心機能が動作していない
   - 常に同じ軌跡（右側のC）が選ばれるため、予測的アルゴリズムの意味がない

2. **最適性の欠如**
   - 11個の選択肢のうち1個しか使われない
   - 状況に応じた最適な軌跡選択ができていない
   - リアクティブアルゴリズム（ThomasAPF）以下の性能になっている可能性

3. **右側Cの軌跡が問題を引き起こす**
   - 常に右カーブが選ばれる
   - この軌跡がエンドポイントCに一致する
   - これが「直進してしまう」問題の直接的な原因

### 次の調査ステップ

**優先順位1: フィルタリング後の軌跡数を確認**

```csharp
// LemniscatePathPredictor.cs:241付近に追加
Debug.Log($"[LPP] Generated trajectories: {predictions.Count}");
Debug.Log($"[LPP] Feasible trajectories: {feasibleTrajectories.Count}");
if (feasibleTrajectories.Count == 1)
{
    Debug.LogWarning($"[LPP] Only 1 feasible trajectory! Endpoint: ?");
}
```

**優先順位2: MSEスコアをログ出力**

```csharp
// LemniscatePathPredictor.cs:286付近に追加
foreach (var trajectory in trajectories)
{
    float mse = trajectory.CalculatePathSimilarity(positionHistory);
    Debug.Log($"[LPP] Trajectory MSE: {mse:F3}, Endpoint: ({trajectory.endPosition.x:F2}, {trajectory.endPosition.y:F2})");
}
```

**優先順位3: エンドポイント座標をログ出力**

```csharp
// LemniscatePathPredictor.cs:206付近に追加
for (int i = 0; i < endpoints.Count; i++)
{
    Debug.Log($"[LPP] Endpoint[{i}]: ({endpoints[i].x:F2}, {endpoints[i].y:F2})");
}
```

### 暫定結論

**問題10の根本原因は、右側のCのエンドポイントのみが常に選択されることにある可能性が高い**

- 過去の移動パターンに関わらず、常に同じ軌跡が選ばれる
- この軌跡がエンドポイントCと一致する状況で、アバターが直進する問題が発生
- 他のエンドポイント（A, B, D, E, F）が選ばれれば、問題は発生しない可能性

**確認すべきこと:**
1. フィルタリングで10本が除外されているのか（衝突判定の問題）
2. MSE計算で常に右側のCが最小になるのか（類似度計算の問題）
3. エンドポイント座標が正しく生成されているのか（レムニスケート方程式の問題）

---

## 右側Cエンドポイント偏重問題の詳細分析（2026-01-12）

### Hirt論文との照合による理論的背景

**論文Section 3.2.2: Lemniscate Path Prediction (LPP)の理論**

論文では以下のプロセスが記述されている：

1. **レムニスケート方程式（Eq.1）:**
   ```
   x(t) = A_Lem × sin(t) / (1 + cos²(t))
   y(t) = A_Lem × sin(t)cos(t) / (1 + cos²(t))
   where t ∈ (0, π)
   ```
   - A_Lem: 予測ホライゾン（通常3m）
   - 11個のエンドポイントを等間隔でサンプリング
   - ∞（無限大）の形状の正の半分を使用

2. **Path similarity measure（軌跡類似度測定）:**
   > "From this generated set of trajectories, a single best prediction is isolated
   > using a simple mean-squared error enhanced by a discount factor."

   - MSE（平均二乗誤差）でHMD履歴との類似度を計算
   - 割引係数（discount factor）で新しい履歴を重視
   - **最小MSEの軌跡を選択**

3. **Scene awareness（シーン認識）:**
   - 衝突判定で実行不可能な軌跡を除外
   - 論文Figure 2の例：左側の壁との衝突で左側の対称な予測が除外される

### 実装の詳細分析

#### 1. レムニスケート生成の符号反転（LemniscatePathPredictor.cs:189-191）

**実装コード:**
```csharp
float x_local = size * sinT / denom;
// y_localの符号を反転：左右のエンドポイントを入れ替える
float y_local = -size * sinT * cosT / denom;
```

**問題点:**
- 論文の式では符号反転はない
- コメントでは「左右を入れ替える」とあるが、この意図が不明確
- 座標変換（195-201行目）と組み合わせた時の挙動が複雑

**座標変換:**
```csharp
float angle = Mathf.Atan2(direction.y, direction.x);
float x_world = origin.x + (x_local * cosAngle - y_local * sinAngle);
float y_world = origin.y + (x_local * sinAngle + y_local * cosAngle);
```

**仮説:** y_localの符号反転により、エンドポイントの配置が左右で非対称になっている可能性

#### 2. MSE計算メカニズム（Trajectory.cs:265-311）

**実装は論文に忠実:**
```csharp
// 履歴の各点について、軌跡上の最も近い点との距離を計算
for (int i = 0; i < historyArray.Length; i++)
{
    // 割引係数：新しい履歴ほど重要
    // i=0（最古）→ discount^(N-1)
    // i=N-1（最新）→ discount^0 = 1.0
    int reverseIndex = historyArray.Length - 1 - i;
    float weight = Mathf.Pow(discountFactor, reverseIndex);

    // 軌跡上の最も近い点を見つける（総当たり）
    float minDistance = float.MaxValue;
    foreach (var trajectoryPoint in points)
    {
        float distance = Vector2.Distance(historyPoint, trajectoryPoint);
        if (distance < minDistance)
            minDistance = distance;
    }

    // 重み付きの二乗誤差を加算
    mse += weight * minDistance * minDistance;
    totalWeight += weight;
}

// 正規化
if (totalWeight > 0)
    mse /= totalWeight;
```

**理論的な動作:**
- ユーザーが過去10フレームで直進していた場合 → 直進軌跡（中央A）が最小MSE
- ユーザーが過去10フレームで左にカーブしていた場合 → 左側の軌跡（B, C, D）が最小MSE
- ユーザーが過去10フレームで右にカーブしていた場合 → 右側の軌跡（B, C, D）が最小MSE

**矛盾点:**
- ユーザーが「過去にほぼ直進」しているのに「右側のCが98%選択される」のは理論と一致しない

#### 3. フィルタリング処理（LemniscatePathPredictor.cs:218-242）

```csharp
public List<Trajectory> FilterFeasibleTrajectories(
    List<Trajectory> trajectories,
    SingleSpace space)
{
    List<Trajectory> feasible = new List<Trajectory>();

    foreach (var trajectory in trajectories)
    {
        // 仮想障害物との衝突をチェック（軌跡の前半50%のみ）
        bool hasCollision = trajectory.CheckCollisionWithObstacles(
            space.obstaclePolygons,
            checkRatio: 0.5f
        );

        // トラッキング空間内に収まっているかチェック
        bool withinBounds = trajectory.IsWithinTrackingSpace(
            space.trackingSpace
        );

        if (!hasCollision && withinBounds)
        {
            feasible.Add(trajectory);
        }
    }

    return feasible;
}
```

**論文との整合性:**
- 論文Figure 2Aで示されているように、衝突する軌跡を除外するのは正しい動作

### 根本原因の特定

#### **重要な発見: 履歴と逆方向の軌跡が選ばれている**

**観察された現象:**
- 過去のユーザー経路: **反時計回り（左カーブ）または直進**
- 選択される軌跡: **右側のC（時計回り）**
- これはMSE理論と**完全に矛盾**している

**MSE理論によれば:**
- 左カーブの履歴 → 左カーブの軌跡が最小MSE
- 直進の履歴 → 直進軌跡（中央A）が最小MSE
- 右カーブの履歴 → 右カーブの軌跡が最小MSE

**実際の挙動:**
- 左カーブor直進の履歴 → **右側のCが選ばれる** ❌

→ **MSE計算は機能していない、または選択肢が1つしかない**

---

#### **原因の仮説**

### **仮説1: フィルタリングで右側のCのみが残る（最有力）**

```
【メカニズム】

Step 1: レムニスケート上に11個のエンドポイントを生成
   A（中央）, B（左右）, C（左右）, D（左右）, E（左右）, F（左右）
  ↓
Step 2: 各エンドポイントへのクロソイド軌跡を生成（11本）
  ↓
Step 3: FilterFeasibleTrajectories()で衝突判定
   - 障害物との衝突チェック（CheckCollisionWithObstacles）
   - トラッキング空間内チェック（IsWithinTrackingSpace）
  ↓
Step 4: 実行可能な軌跡リストを取得
   【予想】feasibleTrajectories.Count == 1 （右側のCのみ）
  ↓
Step 5: SelectBestTrajectoryUsingSimilarityMeasure()
   選択肢が1つしかないため、MSEに関係なく右側のCが返される
```

**なぜ右側のCだけが残るのか:**

1. **トラッキング空間の形状問題:**
   - 物理空間（5.5m × 8.0m）の境界が特定の方向に偏っている
   - アバターの位置と向きの組み合わせで、右側のCのエンドポイントだけが境界内に収まる

2. **y_local符号反転の影響:**
   ```csharp
   float y_local = -size * sinT * cosT / denom;  // 論文にない符号反転
   ```
   - この符号反転により、エンドポイントの配置が左右で非対称になる
   - 回転行列適用後、意図しない位置に配置される

3. **座標系の問題:**
   - レムニスケート座標系（ローカル）→ Unity座標系（ワールド）の変換ミス
   - エンドポイントが物理空間外に配置され、IsWithinTrackingSpaceで除外される

---

### **仮説2: エンドポイントラベルと実際の配置の不一致**

**メカニズム:**
- 内部ラベル「左側のC」が、実際には右側に配置される（y軸反転バグ）
- 左カーブの履歴に対してMSE計算が「左側のC」を選択
- しかし可視化では右側に表示される

**検証方法:**
```csharp
// エンドポイント生成時にログ出力
for (int i = 0; i < endpoints.Count; i++)
{
    Debug.Log($"[LPP] Endpoint[{i}]: ({endpoints[i].x:F2}, {endpoints[i].y:F2})");
}
```

左右対称になっているか確認。

---

### **仮説3: クロソイド生成の方向バグ**

`CreateClothoidFromPoseAndPoint()`が、エンドポイント座標に関わらず常に右カーブのクロソイドを生成してしまう可能性。

---

### 論文との整合性

**論文の理論は正しい:**
- MSE計算による軌跡選択は理論的に健全
- Scene awarenessによるフィルタリングも妥当

**実装の問題:**
- フィルタリングで選択肢が1つに絞られてしまう
- MSE計算が実行される前に、実質的な選択が完了している
- これは**論文が想定していない状況**

### Hirt論文での言及

論文Figure 2Aでは、シーン認識により不適切な軌跡を除外する例が示されている：
> "Applying scene awareness removes infeasible trajectories early on and only
> retains valid ones (white). Here, a symmetric prediction to the left is
> discarded due to a collision with the left-hand wall."

**論文が想定していたケース:**
- 左側の壁がある → 左側の軌跡が除外される
- 複数の実行可能な軌跡が残る → MSEで最適なものを選択

**実装で起きているケース（推測）:**
- 何らかの理由で10本の軌跡が除外される
- 右側のCのみが残る
- MSE計算は実行されるが、選択肢が1つしかない
- 結果として、履歴に関係なく右側のCが選ばれる

### 検証方法

**必要なログ出力:**

1. **フィルタリング前後の軌跡数:**
   ```csharp
   // LemniscatePathPredictor.cs:241付近に追加
   Debug.Log($"[LPP] Generated trajectories: {predictions.Count}");
   Debug.Log($"[LPP] Feasible trajectories after filtering: {feasibleTrajectories.Count}");
   ```

2. **各軌跡のMSEスコアとエンドポイント:**
   ```csharp
   // LemniscatePathPredictor.cs:286付近に追加
   int idx = 0;
   foreach (var trajectory in trajectories)
   {
       float mse = trajectory.CalculatePathSimilarity(positionHistory);
       Debug.Log($"[LPP] Trajectory[{idx}] MSE: {mse:F4}, " +
                 $"Endpoint: ({trajectory.endPosition.x:F2}, {trajectory.endPosition.y:F2})");
       idx++;
   }
   Debug.Log($"[LPP] Selected trajectory with minimum MSE: {minMSE:F4}");
   ```

3. **HMD履歴の移動方向:**
   ```csharp
   // positionHistoryから移動ベクトルを計算
   Vector3[] histArray = positionHistory.ToArray();
   Vector3 overallDirection = histArray[histArray.Length-1] - histArray[0];
   Debug.Log($"[LPP] History direction: ({overallDirection.x:F2}, {overallDirection.z:F2})");
   ```

### 解決策の方向性

#### **対策1: フィルタリング前の軌跡数を確認**
```csharp
// LemniscatePathPredictor.cs:241付近
Debug.Log($"[LPP] Generated: {predictions.Count}, Feasible: {feasibleTrajectories.Count}");
if (feasibleTrajectories.Count == 1)
{
    Debug.LogWarning($"[LPP] Only 1 feasible trajectory! This bypasses MSE selection.");
}
```
- まず実態を把握する
- 本当に1本しか残っていないのか確認

#### **対策2: エンドポイント座標の検証**
```csharp
// エンドポイント生成直後にログ出力
for (int i = 0; i < endpoints.Count; i++)
{
    Debug.Log($"[LPP] Endpoint[{i}]: Local({x_local:F2}, {y_local:F2}) " +
              $"→ World({endpoints[i].x:F2}, {endpoints[i].y:F2})");
}
```
- 左右対称に配置されているか確認
- y_local符号反転の影響を検証

#### **対策3: フィルタリング条件の緩和**
- `checkRatio=0.5f`を`0.3f`に変更（軌跡の前半30%のみ衝突判定）
- より多くの軌跡を候補として残す
- ただし、これは対症療法

#### **対策4: y_local符号反転の削除（実験的）**
```csharp
// LemniscatePathPredictor.cs:190を変更
float y_local = size * sinT * cosT / denom;  // 符号反転を削除
```
- 論文の式に忠実にする
- エンドポイント配置が改善されるか確認

#### **対策5: 予測ホライゾン（A_Lem）の調整**
- 現在3m → 2mに縮小
- エンドポイントが物理空間内に収まりやすくなる

### 結論

**右側のCのエンドポイントが98%選択される根本原因（修正版）:**

1. **フィルタリングで選択肢が1つに絞られている（最有力）**
   - 11本の軌跡が生成される
   - FilterFeasibleTrajectories()で10本が除外される
   - 右側のCのみが残る
   - MSE計算は実行されるが、選択肢が1つしかないため無意味

2. **MSE計算の問題ではない**
   - 過去の履歴が「左カーブ」「直進」なのに「右側のC（右カーブ）」が選ばれる
   - これはMSE理論と完全に矛盾
   - つまり、MSE計算の前にすでに選択が完了している

3. **可能性のある技術的原因:**
   - y_local符号反転によるエンドポイント配置の非対称性
   - トラッキング空間境界との位置関係
   - レムニスケート座標系→Unity座標系変換のバグ

**Hirt論文との関係:**
- 論文のアルゴリズム（MSE計算、Scene awareness）は理論的に正しい
- しかし、実装において：
  - エンドポイント配置に問題がある（y_local符号反転）
  - または、トラッキング空間との位置関係で過度にフィルタリングされる
- 論文は「複数の実行可能な軌跡から選択する」前提だった
- 「選択肢が1つしかない」状況は想定外

**検証すべき最優先事項:**
1. フィルタリング後の軌跡数（1本か？11本か？）
2. エンドポイント座標の左右対称性
3. 各軌跡が除外される理由（衝突？境界外？）

---

## 詳細ログ追加（2026-01-13）

### 実施内容

**既存ログの削除:**
- `Redirector.cs:62-65`: SetRotationGain内のログを削除
- `Redirector.cs:86-90`: SetCurvature内のログを削除

**新規ログの追加:**

**優先順位1: フィルタリング後の軌跡数（LemniscatePathPredictor.cs:241-247）**
```csharp
Debug.Log($"[LPP] Generated trajectories: {trajectories.Count}");
Debug.Log($"[LPP] Feasible trajectories: {feasible.Count}");
if (feasible.Count == 1)
{
    Debug.LogWarning($"[LPP] Only 1 feasible trajectory! MSE selection bypassed.");
}
```

**優先順位2: MSEスコア（LemniscatePathPredictor.cs:293-311）**
```csharp
int idx = 0;
foreach (var trajectory in trajectories)
{
    float mse = trajectory.CalculatePathSimilarity(positionHistory);
    Debug.Log($"[LPP] Trajectory[{idx}] MSE: {mse:F4}, " +
              $"Endpoint: ({trajectory.endPosition.x:F2}, {trajectory.endPosition.y:F2})");
    idx++;
}
Debug.Log($"[LPP] Selected trajectory with minimum MSE: {minMSE:F4}");
```

**優先順位3: エンドポイント座標（LemniscatePathPredictor.cs:206-210）**
```csharp
for (int i = 0; i < endpoints.Count; i++)
{
    Debug.Log($"[LPP] Endpoint[{i}]: ({endpoints[i].x:F2}, {endpoints[i].y:F2})");
}
```

### 確認すべきこと

1. **Feasible trajectories の数**
   - 1本のみ → フィルタリングが原因（仮説1が正しい）
   - 複数本 → MSE計算またはエンドポイント配置の問題（仮説2または3）

2. **エンドポイント座標の左右対称性**
   - 左右対称でない → y_local符号反転が原因
   - 左右対称 → トラッキング空間との位置関係が原因

3. **MSEスコアの分布**
   - 右側Cが最小MSE → MSE計算は機能しているが、履歴との不一致
   - 右側Cが最小MSEでない → フィルタリングで1本のみ残っている

---

## ログ分析結果（2026-01-13）

### 重大な発見

**問題1: MSEスコアが全て同一値（0.0492）**

```
[LPP] Trajectory[0] MSE: 0.0492, Endpoint: (4.77, -6.26)
[LPP] Trajectory[1] MSE: 0.0492, Endpoint: (3.95, -6.24)
[LPP] Trajectory[2] MSE: 0.0492, Endpoint: (3.32, -6.84)
[LPP] Trajectory[3] MSE: 0.0492, Endpoint: (3.39, -7.71)
```

**影響:**
- 全ての軌跡で同じMSE値 = 履歴との類似度計算が機能していない
- `if (mse < minMSE)` の条件により、**常に最初の軌跡（Trajectory[0]）が選ばれる**
- 「右側のCが90%選択される」のではなく、「最初の実行可能な軌跡が100%選択される」

**原因の可能性:**
1. `positionHistory`が空または非常に短い（初期化直後）
2. MSE計算ロジックのバグ（`Trajectory.cs:265-311`）
3. 全ての軌跡点が履歴点から等距離（あり得ない）

**問題2: クロソイド生成の高い失敗率**

```
[LPP] Endpoint[0] ~ Endpoint[10]: 11個生成
[LPP] Generated trajectories: 5  ← 5個のみクロソイド生成成功
```

**影響:**
- 11個のエンドポイントから5個しかクロソイド曲線が生成されていない
- 6個（約55%）が生成失敗 = 選択肢が大幅に減少
- `CurvesWrapper.CreateClothoidFromPoseAndPoint()` が失敗している

**問題3: エンドポイント数の変動**

```
1回目: Endpoint[0] ~ Endpoint[10]  (11個)
2回目: Endpoint[0] ~ Endpoint[10]  (11個)
3回目: Endpoint[0] ~ Endpoint[49]  (50個)
```

**影響:**
- `lemniscateEndpoints` パラメータが実行中に変更されている？
- または異なるアルゴリズムが実行されている可能性

### 根本原因の再評価

**当初の仮説は誤り:**
- ✗ フィルタリングで1本のみ残る
- ✗ y_local符号反転で右側に偏る
- ✗ MSE計算で右側Cが最小値

**真の原因:**
1. **MSE計算が機能していない** → 全て同じ値0.0492
2. **最初の軌跡が常に選択される** → `if (mse < minMSE)` の仕組み
3. **その最初の軌跡が偶然「右寄り」のエンドポイント** → 結果的に右に偏る

### 次のステップ

**優先順位A: MSE計算の検証**

`Trajectory.cs:265-311` の `CalculatePathSimilarity` を確認:
```csharp
Debug.Log($"[Trajectory] positionHistory.Count: {positionHistory.Count}");
Debug.Log($"[Trajectory] trajectory.points.Count: {points.Count}");
```

**優先順位B: positionHistoryの内容確認**

`LemniscatePathPredictor.cs:268-280` に追加:
```csharp
if (positionHistory == null || positionHistory.Count == 0)
{
    Debug.LogWarning("[LPP] positionHistory is null or empty!");
    return trajectories[0];
}
Debug.Log($"[LPP] positionHistory.Count: {positionHistory.Count}");
```

---

## MSE計算詳細ログ追加（2026-01-13）

### 実施内容

**LemniscatePathPredictor.cs:293, 297に追加:**
```csharp
Debug.LogWarning("[LPP] positionHistory is null or empty!");
Debug.Log($"[LPP] positionHistory.Count: {positionHistory.Count}");
```

**Trajectory.cs:277, 280-286, 323に追加:**
```csharp
// 履歴数と軌跡点数
Debug.Log($"[Trajectory.MSE] historyArray.Length: {historyArray.Length}, points.Count: {points.Count}");

// 履歴の移動距離
Vector2 firstPos = Utilities.FlattenedPos2D(historyArray[0]);
Vector2 lastPos = Utilities.FlattenedPos2D(historyArray[historyArray.Length - 1]);
float historyDistance = Vector2.Distance(firstPos, lastPos);
Debug.Log($"[Trajectory.MSE] History: first=({firstPos.x:F2},{firstPos.y:F2}), last=({lastPos.x:F2},{lastPos.y:F2}), distance={historyDistance:F2}m");

// 最終MSE値
Debug.Log($"[Trajectory.MSE] Final MSE: {mse:F6}, totalWeight: {totalWeight:F2}");
```

### 確認すべきこと

1. **positionHistory.Count** - 1～2なら履歴不足が原因
2. **historyDistance** - 0.0mに近いなら、ほぼ同じ位置の履歴 = 全軌跡で同じMSE
3. **historyArray.Length vs points.Count** - 比率の確認

---

## 根本原因の特定完了（2026-01-13）

### ログ分析結果

```
[Trajectory.MSE] History: first=(3.40,0.03), last=(3.59,-0.25), distance=0.34m
[Trajectory.MSE] Final MSE: 0.043602, totalWeight: 4.46
[LPP] Trajectory[0] MSE: 0.0436, Endpoint: (3.73, -2.39)
[LPP] Trajectory[1] MSE: 0.0436, Endpoint: (4.38, -2.88)
[LPP] Trajectory[2] MSE: 0.0436, Endpoint: (5.24, -2.76)
[LPP] Trajectory[3] MSE: 0.0436, Endpoint: (5.69, -2.01)
[LPP] Trajectory[4] MSE: 0.0436, Endpoint: (5.50, -1.22)
```

### 問題の本質

**履歴の移動距離が極端に短い（0.34m = 34cm）**

**詳細:**
- positionHistory.Count = 10（10個の履歴点がある）
- しかし、10個の点がたった0.34mの範囲に集中
- first=(3.40, 0.03) → last=(3.59, -0.25)
  - X軸: 0.19mの変化
  - Y軸: 0.28mの変化
- **全ての軌跡でMSE値が完全に同一（0.043602）**

**なぜMSEが同じになるのか:**

1. 履歴の10個の点が「ほぼ1点」に集中している
2. 予測軌跡の開始点は現在位置（3.59, -0.25）付近
3. どの軌跡も、この「点のような履歴」から2～3m離れた場所に伸びている
4. 履歴が点状なので、どの軌跡も履歴から**ほぼ等距離**にある
5. したがって、MSE計算結果が全て同じ値になる

**図解:**

```
履歴（10個の点が0.34mの範囲に集中）
  ●●●●●●●●●●  (3.40, 0.03) → (3.59, -0.25)
   ↓
   現在位置

予測軌跡（全て2～3m先へ伸びる）
  ――――→ Trajectory[0] (3.73, -2.39)
  ――――→ Trajectory[1] (4.38, -2.88)
  ――――→ Trajectory[2] (5.24, -2.76)
  ――――→ Trajectory[3] (5.69, -2.01)
  ――――→ Trajectory[4] (5.50, -1.22)

履歴が「点」なので、どの軌跡も等距離 → MSE全て同じ
```

### なぜ0.34mしか移動していないのか

**原因1: 履歴の時間範囲が短すぎる**
- 50fps環境で10フレーム = 0.2秒分の履歴のみ
- SimulatedWalkerの移動速度が約1.7m/s（歩行）の場合
- 0.2秒 × 1.7m/s = 0.34m ← ログと一致

**原因2: 毎フレーム履歴を追加している**
- `positionHistory`に毎フレーム現在位置を追加
- 50fpsなら10フレーム = たった0.2秒分
- **時間的に短すぎて、移動パターンが捉えられない**

### Hirt論文との比較

**論文の想定（Section 3.2.2）:**
- ユーザーの「移動パターン」を履歴から抽出
- 左カーブしているなら左カーブの軌跡を選択
- 右カーブしているなら右カーブの軌跡を選択
- **移動パターンを捉えるには、十分な距離の履歴が必要**

**現在の実装:**
- 0.34mの履歴 = 歩幅0.5～1歩分
- これでは移動パターン（カーブ方向）が分からない
- **MSE計算は正しいが、入力データが不適切**

### 解決策

**対策1: positionHistoryの蓄積間隔を変更（推奨）**

現在: 毎フレーム追加
```csharp
// 毎フレーム実行 (50fps)
positionHistory.Enqueue(currentPosition);
if (positionHistory.Count > 10)
    positionHistory.Dequeue();
```

変更案: 一定距離移動したときのみ追加
```csharp
// 前回の記録位置から0.2m以上移動したら追加
if (Vector3.Distance(currentPosition, lastRecordedPosition) >= 0.2f)
{
    positionHistory.Enqueue(currentPosition);
    lastRecordedPosition = currentPosition;
    if (positionHistory.Count > 10)
        positionHistory.Dequeue();
}
```

効果: 10個の履歴点で2m以上の範囲をカバー → 移動パターンが捉えられる

**対策2: 履歴バッファサイズを増やす**

現在: 10個（0.2秒、0.34m）
変更: 50個（1秒、1.7m）

効果: より長期間の移動パターンを考慮できる

**対策3: 履歴距離が短い場合のフォールバック**

```csharp
float historyDistance = Vector2.Distance(firstPos, lastPos);
if (historyDistance < 1.0f)
{
    // 履歴が短い場合は中央の軌跡（直進）を選択
    return trajectories[trajectories.Count / 2];
}
```

### 結論

**「右側のCが90%選択される」本当の原因:**

1. MSE計算は正しく機能している ✓
2. しかし、履歴の移動距離が0.34mと極端に短い ✗
3. 履歴が「点」のため、全ての軌跡で同じMSE値になる ✗
4. `if (mse < minMSE)` により、常に最初の軌跡が選ばれる ✗
5. フィルタリング後の最初の軌跡が「右寄り」だったため、結果的に右に偏る ✗

**これはアルゴリズムのバグではなく、データ不足の問題である**

対策1（履歴の蓄積間隔変更）を実装することを強く推奨します。

---

## 履歴バッファサイズ増加による対策実施（2026-01-13）

### 実施内容

**PredRedLPP_Redirector.cs:36の変更:**
```csharp
// 変更前
private const int MAX_HISTORY_SIZE = 10;  // 履歴の最大サイズ

// 変更後
private const int MAX_HISTORY_SIZE = 50;  // 履歴の最大サイズ（10→50に変更：移動パターン捕捉のため）
```

**効果:**
- 10個（0.2秒、0.34m）→ 50個（1秒、1.7m）
- 50fps環境で1秒分の履歴 = 約1.7mの移動距離をカバー
- 移動パターン（左カーブ、右カーブ、直進）を捉えられる

**不要なログの削除:**
- `Trajectory.cs:277`: historyArray.Lengthとpoints.Countのログ削除
- `Trajectory.cs:280-286`: 履歴の最初と最後の位置の詳細ログ削除
- `Trajectory.cs:323`: 最終MSE値のログ削除
- `LemniscatePathPredictor.cs:206-210`: エンドポイント座標の詳細ログ削除
- `LemniscatePathPredictor.cs:297`: positionHistory.Countの通常ログ削除

**残したログ（見るべきログ）:**
- `LemniscatePathPredictor.cs:242-247`: フィルタリング後の軌跡数
- `LemniscatePathPredictor.cs:293`: positionHistoryがnullまたは空の場合の警告
- `LemniscatePathPredictor.cs:300-301`: 各軌跡のMSEとエンドポイント
- `LemniscatePathPredictor.cs:311`: 選択されたMSE値

### 期待される結果

**変更前:**
```
[Trajectory.MSE] History: distance=0.34m
[LPP] Trajectory[0] MSE: 0.0436
[LPP] Trajectory[1] MSE: 0.0436  ← 全て同じ
[LPP] Trajectory[2] MSE: 0.0436
```

**変更後（期待）:**
```
[LPP] Trajectory[0] MSE: 0.0523  ← 軌跡ごとに異なる
[LPP] Trajectory[1] MSE: 0.0312  ← 最小MSE
[LPP] Trajectory[2] MSE: 0.0789
[LPP] Selected trajectory with minimum MSE: 0.0312
```

履歴距離が1.7mになることで、各軌跡のMSE値が区別でき、ユーザーの移動パターンに合った軌跡が選択されるようになる。

---

## 履歴バッファサイズ増加後の動作確認結果（2026-01-13）

### 実験結果

**MAX_HISTORY_SIZE = 50（1秒、理論値1.7m）に変更後:**

```
[LPP] Generated trajectories: 5
[LPP] Feasible trajectories: 5
[LPP] Trajectory[0] MSE: 0.0120, Endpoint: (-1.34, -4.56)
[LPP] Trajectory[1] MSE: 0.0120, Endpoint: (-0.59, -4.90)
[LPP] Trajectory[2] MSE: 0.0120, Endpoint: (0.22, -4.59)
[LPP] Trajectory[3] MSE: 0.0120, Endpoint: (0.50, -3.76)
[LPP] Trajectory[4] MSE: 0.0120, Endpoint: (0.13, -3.03)
[LPP] Selected trajectory with minimum MSE: 0.0120
```

### 問題の持続

**観察事実:**
- 全ての軌跡で依然としてMSE値が完全に同一（0.0120）
- MAX_HISTORY_SIZE = 10 → 50に増やしても状況は改善せず
- 常に最初の軌跡（Trajectory[0]）が選択される状況が継続

**MSE値の変化:**
- 変更前: 0.0436（10個の履歴）
- 変更後: 0.0120（50個の履歴）
- MSE値自体は減少したが、**全軌跡で同一値のまま**

### 根本原因の再分析

**仮説1: 履歴の実際の移動距離が依然として短い**

可能性:
- SimulatedWalkerの移動速度が想定（1.7m/s）より遅い
- またはほとんど静止している状態でテストされた
- 50個の履歴があっても、実際の移動距離が0.5m程度しかない

検証が必要:
```csharp
// Trajectory.cs:CalculatePathSimilarity内に再追加
Vector2 firstPos = Utilities.FlattenedPos2D(historyArray[0]);
Vector2 lastPos = Utilities.FlattenedPos2D(historyArray[historyArray.Length - 1]);
float historyDistance = Vector2.Distance(firstPos, lastPos);
Debug.Log($"[MSE] History distance: {historyDistance:F2}m with {historyArray.Length} points");
```

**仮説2: MSE計算アルゴリズムの根本的問題**

問題点:
- 現在のMSE計算（`Trajectory.cs:265-311`）は「履歴の各点から最も近い軌跡点までの距離」を計算
- しかし、予測軌跡は全て「現在位置から3m前方」に伸びている
- 履歴は「現在位置周辺（後方）」に集中している
- つまり、**履歴と軌跡が空間的に重ならない**

図解:
```
過去の履歴（50個の点）    現在位置         予測軌跡（5本）
●●●●●●●●●●         ◉      ========→ Trajectory[0]
                               ========→ Trajectory[1]
<------ 後方 ------>   ↑   <----- 前方3m ----->
                      ここ

履歴と軌跡が重ならない → 全軌跡が履歴から等距離 → MSE全て同じ
```

**仮説3: predictionHorizon（3m）と履歴範囲の不一致**

論文の前提:
- 予測軌跡は過去の移動パターンを「未来に延長」するもの
- 過去の軌跡に近い形状の予測軌跡を選択する

現在の実装:
- predictionHorizon = 3m（前方3m先へ予測）
- 履歴範囲 ≈ 1.7m（後方の移動履歴）
- **過去と未来が空間的に分離している**

### Hirt論文での想定との乖離

**論文Section 3.2.2の想定:**
> "From this generated set of trajectories, a single best prediction is isolated
> using a simple mean-squared error enhanced by a discount factor."

論文が想定していたこと:
1. ユーザーが過去1～2秒間、左にカーブしながら歩いていた
2. その軌跡の「延長線上」に予測軌跡を生成
3. 過去の軌跡と予測軌跡が**空間的に重なる部分がある**
4. MSE計算で、過去の軌跡形状に近い予測を選択

現在の実装で起きていること:
1. 過去の履歴は現在位置の「後方」にある
2. 予測軌跡は現在位置の「前方3m」に伸びる
3. **過去と未来が空間的に分離**
4. MSE計算で全ての予測軌跡が等距離になる

### MSE計算方式の問題

**現在の方式（Trajectory.cs:280-304）:**
```
履歴の各点について、軌跡上の最も近い点との距離を計算
→ 履歴点と軌跡点の「絶対位置」の比較
```

**問題点:**
- 履歴（後方）と予測（前方）が重ならないため、意味のある比較にならない
- 全ての予測軌跡が履歴から約3m離れている → 全て同じMSE

**本来あるべき方式:**
```
履歴の「形状・パターン」（カーブ方向、曲率）を抽出
→ 予測軌跡の「形状・パターン」と比較
→ 似た形状の軌跡を選択
```

例:
- 過去の履歴: 左に10度カーブしながら歩いた
- 予測軌跡A: 左に15度カーブ → MSE低い（類似）
- 予測軌跡B: 右に15度カーブ → MSE高い（非類似）

### 結論

**「右側のCが90%選択される」問題は解決せず:**

1. ✗ 履歴バッファサイズを50に増やしても効果なし
2. ✗ MSE値が依然として全軌跡で同一
3. ✗ 常に最初の軌跡が選択される状況が継続

**根本原因（確定）:**

MSE計算アルゴリズムの設計上の欠陥:
- 「履歴の絶対位置」と「予測軌跡の絶対位置」を比較している
- しかし、履歴（後方）と予測（前方）は空間的に重ならない
- 結果として、全ての予測軌跡が等距離 → MSE全て同じ

**Hirt論文との乖離:**
- 論文は「軌跡の形状・パターン」の類似度を想定
- 実装は「絶対位置」の比較になっている
- これは論文の意図と完全に異なる

**解決には以下が必要:**

1. **MSE計算方式の根本的な見直し**
   - 絶対位置の比較ではなく、形状・パターンの比較
   - 曲率、進行方向の変化率などを比較

2. **または、予測開始点を過去に設定**
   - 現在位置ではなく、0.5秒前の位置から予測を開始
   - これにより履歴と予測軌跡が重なる

3. **または、方向ベクトルの類似度を使用**
   - 履歴の進行方向と予測軌跡の進行方向を比較
   - 絶対位置ではなく、相対的な向きで評価

**現状の評価:**

PredRedLPP（Lemniscate Path Prediction）の予測軌跡選択機能は**機能していない**。
履歴との類似度計算が設計上の問題により無効化されており、常にフィルタリング後の最初の軌跡が選択される。
これは論文で期待される「ユーザーの移動パターンに基づく予測」が実現できていないことを意味する。
