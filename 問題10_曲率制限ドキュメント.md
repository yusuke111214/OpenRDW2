# 問題10:最大曲率時のリダイレクション不全

## 問題の症状
- 曲率を大きく加えた軌跡が最良軌跡T_redとなるときアバターが直進する
- 緩い曲率では正常に動作する
- ログ上はすべて正常に見える
- APF矢印とシアン軌跡は正しい方向を示している
- 視覚的な錯覚ではなく、実際に直進している

## 根本原因(確定)

**MSE計算アルゴリズムの設計上の欠陥により予測軌跡選択が機能していない**

### 問題の構造

1. **履歴と予測軌跡が空間的に分離**
   ```
   過去の履歴(50個の点)    現在位置         予測軌跡(5本)
   ●●●●●●●●●●         ◉      ========→ Trajectory[0]
                                  ========→ Trajectory[1]
   <------ 後方 ------>   ↑   <----- 前方3m ----->
                         ここ
   ```
   - 履歴:現在位置の後方に存在
   - 予測軌跡:現在位置から前方3mへ伸びる
   - 両者が重ならないため距離ベースのMSE計算が無意味

2. **全軌跡で同一MSE値**
   ```
   [LPP] Trajectory[0] MSE: 0.0120, Endpoint: (-1.34, -4.56)
   [LPP] Trajectory[1] MSE: 0.0120, Endpoint: (-0.59, -4.90)
   [LPP] Trajectory[2] MSE: 0.0120, Endpoint: (0.22, -4.59)
   [LPP] Trajectory[3] MSE: 0.0120, Endpoint: (0.50, -3.76)
   [LPP] Trajectory[4] MSE: 0.0120, Endpoint: (0.13, -3.03)
   ```
   - 履歴の移動距離が0.34m(点状)に集中
   - 全予測軌跡が履歴から等距離 → MSE値が同一

3. **常に最初の軌跡が選択される**
   - `if (mse < minMSE)`の条件により、MSE同一時は最初の軌跡(Trajectory[0])が選ばれる
   - フィルタリング後の最初の軌跡が偶然「右寄り」→ 結果的に右に偏る

### 履歴距離不足の詳細

```
[MSE] History: first=(3.40,0.03), last=(3.59,-0.25), distance=0.34m
```

**なぜ0.34mしか移動していないのか:**
- 50fps環境で10フレーム = 0.2秒分の履歴のみ
- SimulatedWalkerの移動速度が約1.7m/s(歩行)の場合
- 0.2秒 × 1.7m/s = 0.34m ← ログと一致

**MAX_HISTORY_SIZE = 50に増加しても改善せず:**
- 10個(0.2秒、0.34m)→ 50個(1秒、1.7m)に変更
- MSE値は0.0436→0.0120に減少したが、**全軌跡で同一値のまま**
- 履歴バッファサイズ増加では根本的に解決しない

### Hirt論文との乖離

**論文Section 3.2.2の想定:**
- ユーザーが過去1～2秒間、左にカーブしながら歩いていた
- その軌跡の「延長線上」に予測軌跡を生成
- 過去の軌跡と予測軌跡が**空間的に重なる部分がある**
- MSE計算で、過去の軌跡形状に近い予測を選択

**現在の実装の問題:**
- 過去の履歴は現在位置の「後方」にある
- 予測軌跡は現在位置の「前方3m」に伸びる
- **過去と未来が空間的に分離**
- MSE計算(`Trajectory.cs:265-311`)は「履歴の絶対位置」と「予測軌跡の絶対位置」を比較
- 論文が想定した「軌跡の形状・パターン」の類似度ではない

---

## 調査の経緯

### 第1～5回ログ分析(初期調査)

**第1回修正:衝突判定の緩和**
- `Trajectory.cs:140`: `CheckCollisionWithObstacles()`に`checkRatio`パラメータ追加
- `LemniscatePathPredictor.cs:229`: 軌跡の最初の50%のみ衝突判定(`checkRatio=0.5f`)
- 結果:リアクティブモードへの切り替わり問題は解決

**第2～5回修正:詳細ログの追加**
- `Redirector.cs:84-87`: SetCurvature()で最大曲率のログ追加
- `Redirector.cs:39-42`: ApplyGains()で回転のログ追加
- `GlobalConfiguration.cs:560`: スタックトレースを無効化

**第5回ログ分析結果:**
- 最大曲率 `curvature=0.133 rad/m` が継続的に選択 ✅
- `SetCurvature()`: `applied=True` ✅
- `rotationGC = 0.27° ～ 0.36°/frame` ✅
- 理論値(15.3°/秒)と完全に一致 ✅
- **しかし実際にはアバターが直進する** ❌
- **ログは正常なのに実際の動作が異なる矛盾**

### エンドポイント可視化による発見(2026-01-12)

**実装:**
- `PredRedLPP_Redirector.cs`にエンドポイント可視化機能を追加
- レムニスケート上の11個のエンドポイントを赤い球体で表示

**エンドポイント配置:**
```
        F E D C B A B C D E F
        ←――――――――+――――――――→
        左側      中央     右側
```
- A:真正面(直進軌跡)
- B:小さい曲率(左右に2個)
- C:中程度の曲率(左右に2個)
- D, E, F:大きな曲率(左右に各3個)

**重大な発見:右側のCのみが99%使用される**

| 状況 | 選択されるエンドポイント |
|-----|----------------------|
| アバターが直進中 | **右側のC** |
| アバターが左に曲がった後 | **右側のC** |
| アバターが右に曲がった後 | **右側のC** |
| 壁に近づいている時 | **右側のC** |

- 過去の移動方向に関わらず、常に同じ軌跡が選ばれる
- 他の10個のエンドポイントはほぼ使用されない
- **予測機能が動作していない**

### MSE問題の特定(2026-01-13)

**詳細ログ追加:**

優先順位1: フィルタリング後の軌跡数(`LemniscatePathPredictor.cs:241-247`)
```csharp
Debug.Log($"[LPP] Generated trajectories: {trajectories.Count}");
Debug.Log($"[LPP] Feasible trajectories: {feasible.Count}");
if (feasible.Count == 1)
{
    Debug.LogWarning($"[LPP] Only 1 feasible trajectory! MSE selection bypassed.");
}
```

優先順位2: MSEスコア(`LemniscatePathPredictor.cs:293-311`)
```csharp
int idx = 0;
foreach (var trajectory in trajectories)
{
    float mse = trajectory.CalculatePathSimilarity(positionHistory);
    Debug.Log($"[LPP] Trajectory[{idx}] MSE: {mse:F4}, " +
              $"Endpoint: ({trajectory.endPosition.x:F2}, {trajectory.endPosition.y:F2})");
    idx++;
}
Debug.Log($"[LPP] Selected trajectory with minimum MSE: {minMSE:F4}");
```

**ログ分析結果:**

問題1: **MSEスコアが全て同一値**
```
[LPP] Trajectory[0] MSE: 0.0492, Endpoint: (4.77, -6.26)
[LPP] Trajectory[1] MSE: 0.0492, Endpoint: (3.95, -6.24)
[LPP] Trajectory[2] MSE: 0.0492, Endpoint: (3.32, -6.84)
```
- 全ての軌跡で同じMSE値 = 履歴との類似度計算が機能していない
- 常に最初の軌跡(Trajectory[0])が選ばれる

問題2: **クロソイド生成の高い失敗率**
```
[LPP] Endpoint[0] ~ Endpoint[10]: 11個生成
[LPP] Generated trajectories: 5  ← 5個のみクロソイド生成成功
```
- 11個のエンドポイントから5個しかクロソイド曲線が生成されていない
- 6個(約55%)が生成失敗 = 選択肢が大幅に減少

**履歴距離の詳細調査:**

追加ログ(`Trajectory.cs:280-286`)
```csharp
Vector2 firstPos = Utilities.FlattenedPos2D(historyArray[0]);
Vector2 lastPos = Utilities.FlattenedPos2D(historyArray[historyArray.Length - 1]);
float historyDistance = Vector2.Distance(firstPos, lastPos);
Debug.Log($"[MSE] History: first=({firstPos.x:F2},{firstPos.y:F2}), " +
          $"last=({lastPos.x:F2},{lastPos.y:F2}), distance={historyDistance:F2}m");
```

結果:
```
[MSE] History: first=(3.40,0.03), last=(3.59,-0.25), distance=0.34m
```
- 10個の点がたった0.34mの範囲に集中
- X軸:0.19m、Y軸:0.28mの変化のみ
- 履歴が「ほぼ1点」に集中 → 全軌跡が等距離 → MSE同一

### 対策実施と結果

**対策:履歴バッファサイズ増加(`PredRedLPP_Redirector.cs:36`)**
```csharp
// 変更前
private const int MAX_HISTORY_SIZE = 10;

// 変更後
private const int MAX_HISTORY_SIZE = 50;  // 10→50に変更
```

**結果:改善せず**
```
[LPP] Trajectory[0] MSE: 0.0120, Endpoint: (-1.34, -4.56)
[LPP] Trajectory[1] MSE: 0.0120, Endpoint: (-0.59, -4.90)
[LPP] Trajectory[2] MSE: 0.0120, Endpoint: (0.22, -4.59)
```
- MSE値は0.0436→0.0120に減少
- しかし**全軌跡で同一値のまま**
- 常に最初の軌跡が選択される状況が継続

---

## MSE計算方式の問題

### 現在の実装(`Trajectory.cs:265-311`)

```csharp
public float CalculatePathSimilarity(Queue<Vector3> positionHistory)
{
    // 履歴の各点について、軌跡上の最も近い点との距離を計算
    for (int i = 0; i < historyArray.Length; i++)
    {
        // 割引係数:新しい履歴ほど重要
        int reverseIndex = historyArray.Length - 1 - i;
        float weight = Mathf.Pow(discountFactor, reverseIndex);

        // 軌跡上の最も近い点を見つける(総当たり)
        float minDistance = float.MaxValue;
        foreach (var trajectoryPoint in points)
        {
            float distance = Vector2.Distance(historyPoint, trajectoryPoint);
            if (distance < minDistance)
                minDistance = distance;
        }

        // 重み付きの二乗誤差を加算
        mse += weight * minDistance * minDistance;
        totalWeight += weight;
    }

    // 正規化
    if (totalWeight > 0)
        mse /= totalWeight;
}
```

**問題点:**
- 履歴点と軌跡点の「絶対位置」を比較している
- 履歴(後方)と予測(前方)が重ならないため、意味のある比較にならない
- 全ての予測軌跡が履歴から約3m離れている → 全て同じMSE

### 本来あるべき方式

```
履歴の「形状・パターン」(カーブ方向、曲率)を抽出
→ 予測軌跡の「形状・パターン」と比較
→ 似た形状の軌跡を選択
```

例:
- 過去の履歴:左に10度カーブしながら歩いた
- 予測軌跡A:左に15度カーブ → MSE低い(類似)
- 予測軌跡B:右に15度カーブ → MSE高い(非類似)

---

## 解決策の方向性

### 対策1: MSE計算方式の根本的な見直し(推奨)

**方法:**
- 絶対位置の比較ではなく、形状・パターンの比較
- 曲率、進行方向の変化率などを比較

**実装例:**
```csharp
public float CalculateShapeSimilarity(Queue<Vector3> positionHistory)
{
    // 履歴から移動方向ベクトルを計算
    Vector3[] histArray = positionHistory.ToArray();
    List<Vector2> historyDirections = new List<Vector2>();
    for (int i = 1; i < histArray.Length; i++)
    {
        Vector2 dir = new Vector2(
            histArray[i].x - histArray[i-1].x,
            histArray[i].z - histArray[i-1].z
        ).normalized;
        historyDirections.Add(dir);
    }

    // 予測軌跡の方向ベクトルを計算
    List<Vector2> trajectoryDirections = new List<Vector2>();
    for (int i = 1; i < points.Count; i++)
    {
        Vector2 dir = (points[i] - points[i-1]).normalized;
        trajectoryDirections.Add(dir);
    }

    // 方向ベクトルの類似度を計算(内積)
    float similarity = 0f;
    int minLength = Mathf.Min(historyDirections.Count, trajectoryDirections.Count);
    for (int i = 0; i < minLength; i++)
    {
        float dotProduct = Vector2.Dot(historyDirections[i], trajectoryDirections[i]);
        similarity += dotProduct;
    }

    return -similarity;  // 小さいほど類似(MSEとの整合性)
}
```

### 対策2: 予測開始点を過去に設定

**方法:**
- 現在位置ではなく、0.5秒前の位置から予測を開始
- これにより履歴と予測軌跡が空間的に重なる

**実装箇所:**
- `LemniscatePathPredictor.cs:250-260`付近
- クロソイド生成時の開始位置を調整

### 対策3: 方向ベクトルの類似度を使用

**方法:**
- 履歴の進行方向と予測軌跡の進行方向を比較
- 絶対位置ではなく、相対的な向きで評価

**実装例:**
```csharp
// 履歴の全体的な移動方向を計算
Vector3[] histArray = positionHistory.ToArray();
Vector3 overallDirection = histArray[histArray.Length-1] - histArray[0];
Vector2 historyDir = new Vector2(overallDirection.x, overallDirection.z).normalized;

// 予測軌跡の方向を計算
Vector2 trajectoryDir = (trajectory.endPosition - trajectory.startPosition).normalized;

// 内積で類似度を計算(-1～1、1が最も類似)
float similarity = Vector2.Dot(historyDir, trajectoryDir);
float mse = 1.0f - similarity;  // 0～2の範囲、小さいほど類似
```

### 対策4: 履歴距離が短い場合のフォールバック

**実装例:**
```csharp
// LemniscatePathPredictor.cs:268-280付近に追加
public Trajectory SelectBestTrajectoryUsingSimilarityMeasure(
    List<Trajectory> trajectories,
    Queue<Vector3> positionHistory)
{
    if (positionHistory == null || positionHistory.Count < 3)
    {
        return trajectories[trajectories.Count / 2];  // 中央(直進)を選択
    }

    // 履歴の移動距離を確認
    Vector3[] histArray = positionHistory.ToArray();
    Vector2 firstPos = Utilities.FlattenedPos2D(histArray[0]);
    Vector2 lastPos = Utilities.FlattenedPos2D(histArray[histArray.Length - 1]);
    float historyDistance = Vector2.Distance(firstPos, lastPos);

    if (historyDistance < 1.0f)
    {
        // 履歴が短い場合は中央の軌跡(直進)を選択
        return trajectories[trajectories.Count / 2];
    }

    // 通常のMSE計算
    // ...
}
```

---

## 関連コード箇所

### 主要ファイル

1. **LemniscatePathPredictor.cs**
   - 165-207行: レムニスケートエンドポイント生成
   - 218-242行: フィルタリング処理
   - 268-298行: MSEベースの軌跡選択

2. **Trajectory.cs**
   - 265-311行: MSE計算(`CalculatePathSimilarity`)

3. **PredRedLPP_Redirector.cs**
   - 36行: `MAX_HISTORY_SIZE`定義
   - 履歴の蓄積処理

4. **Redirector.cs**
   - `SetCurvature()`メソッド
   - `ApplyGains()`メソッド

### レムニスケート方程式の実装

`LemniscatePathPredictor.cs:189-191`
```csharp
float x_local = size * sinT / denom;
// y_localの符号を反転:左右のエンドポイントを入れ替える
float y_local = -size * sinT * cosT / denom;
```

**注意:**
- 論文の式では符号反転はない
- この符号反転の意図が不明確
- エンドポイント配置に影響している可能性

---

## 結論（更新）

**問題10は解決済み**

1. **原因:**
   - MSE計算が「絶対位置」を比較しており、履歴(後方)と予測(前方)が空間的に分離して同点になっていた
   - 同点時に最初の軌跡が選ばれ続け、予測が固定化していた

2. **修正:**
   - `Trajectory.CalculatePathSimilarity` を進行方向ベクトル列のMSE（1 - dot）に変更
   - 履歴不足時のみ最初の軌跡を返す従来フォールバックに戻し、不要なデバッグログや暫定制限を削除
   - アクションセット/衝突判定/履歴サイズを論文の想定に合わせて復元

3. **結果:**
   - 予測軌跡がA/B/Cのエンドポイントを偏りなく選択
   - 大きな曲率アクションでもカーブ移動が成立

4. **論文との整合:**
   - MSE + 割引係数という形式を維持しつつ、「軌跡形状の類似性」に沿った評価となり、論文の意図に一致
